# Changelog

## Unreleased

Initial analyser foundation, rule catalogue, reporting contracts, and local quality gates.

- **Modernisation sweep (M37)** - Narrowed honest PHPDoc shapes, added readonly and runtime type refinements, and converted clearer internal long calls to named arguments. The remaining M37 modernisation residuals are documented exemptions: 16 decoded YAML/JSON boundary `array<array-key, mixed>` validator docs and 5 Symfony Console constructor callsites intentionally left positional to avoid coupling to framework parameter names. (`src/Config/`, `src/Mutation/InfectionReportParser.php`, `src/Command/`, `.goat-flow/logs/sessions/2026-05-12-m37-modernisation-naming.md`)
- **Naming sweep (M37)** - Cleared `naming.parameter-type-name`, `naming.generic-method`, `naming.identifier-quality`, and `naming.short-variable` from the M37 target subset. The remaining 3 `naming.boolean-prefix` findings are public API names deferred to a later deprecation cycle rather than silently breaking callers. (`src/`, `.gruff.yaml`, `.goat-flow/logs/rules/m37-after/`)
- **Control-flow comment policy (M37)** - Removed `docs.return-comment` and `docs.continue-comment` from the v0.1 catalogue after dogfooding showed roughly 1.8k combined self-findings that encouraged ceremony comments. Future reconsideration is routed through the 0.2 control-flow comment decision/spike milestones, and ADR-006 records the durable policy. (`src/Rule/Docs/`, `src/Rule/RuleRegistry.php`, `tests/Rule/Docs/DocsRulesTest.php`, `.goat-flow/decisions/ADR-006-control-flow-comment-policy.md`)
- **Control-flow comment severity calibration (M32, superseded by M37)** - `docs.return-comment` and `docs.continue-comment` were temporarily downgraded from `Severity::Warning` to `Severity::Advisory` after they emitted 1803 findings on gruff's own `src/` together (1463 + 340). M37 later superseded this interim calibration by deleting the rules from v0.1. Naming.parameter-type-name's project-specific `ignoredParameterNames` list also expanded to cover gruff's AST/framework conventions (`finder`, `method`, `call`, `registry`, `config`, `definition`, `scope`, `report`, `options`), dropping the rule's count on `src/` from 549 to 92. (`.gruff.yaml`, `tests/Rule/Docs/DocsRulesTest.php`)
- **Complexity, size, and dead-code refactors (M36)** - Refactored the 13 complexity errors and 3 parameter-count errors from the 2026-05-12 self-analysis down to 0 without relaxing `.gruff.yaml` thresholds. Added focused prep tests for uncovered CLI/config branches, introduced `SummaryReportData` and `IdentifierFindingContext` DTOs, split large command/config/docs/naming methods into focused helpers, and inlined 29 dead-code findings (27 one-line wrappers, 1 unused import, 1 redundant variable). The final M36 dogfood snapshot reports `error=0` on `src/`, `complexity_size_total=0`, and 17 documented intent-bearing `waste.one-line-method` exemptions captured in ADR-005. (`src/Command/`, `src/Config/`, `src/Rule/`, `src/Reporting/`, `src/Scoring/`, `tests/`, `.goat-flow/decisions/ADR-005-intent-bearing-one-line-methods.md`)
- **PHPDoc tag completeness (M35)** - Added missing PHPDoc tag coverage across `src/` for `@param`, `@throws`, local `@var` assertion descriptions, and promoted-property constructor `@param` gaps. The 2026-05-12 dogfood scan now reports 0 for `docs.missing-param-tag`, `docs.missing-return-tag`, `docs.missing-throws-tag`, `docs.var-annotation-description`, promoted-property `docs.missing-property-phpdoc`, and `docs.useless-phpdoc`; `modernisation.phpdoc-mixed-overuse` remains at 54 and is deferred to M37. (`src/`, `.goat-flow/decisions/ADR-004-public-phpdoc-template.md`)
- **Structural PHPDoc coverage (M34)** - Added meaningful structural PHPDoc across `src/` for file/type coverage, declared properties, class constants, and enum cases. The 2026-05-12 dogfood scan dropped true structural findings from 640 to 0 (`docs.missing-file-phpdoc`, `docs.missing-class-phpdoc`, declared-property `docs.missing-property-phpdoc`, and `docs.missing-constant-phpdoc`). The 208 constructor-promoted property `@param` gaps are intentionally deferred to M35 tag completeness. (`src/`, `.goat-flow/decisions/ADR-004-public-phpdoc-template.md`)
- **Method PHPDoc coverage (M33)** - Added intent-focused method PHPDoc across `src/`, clearing `docs.missing-public-phpdoc` from 658 findings to 0 while keeping `docs.useless-phpdoc` and `docs.missing-return-tag` at 0. The historical rule ID remains compatibility-only: the sweep documents every method declaration shape used by gruff, not just public APIs, and ADR-004 records the reusable docblock templates for later PHPDoc milestones. (`src/`, `.goat-flow/decisions/ADR-004-public-phpdoc-template.md`)
- **New rule correctness and parameter-name calibration (M32)** - Fixed three issues surfaced by the 2026-05-11 goat-review on commits `f058485` / `285e805`. **Phase 1 (correctness):** `docs.var-annotation-description` switched from a token-stream walker to AST traversal via `NodeFinder` so attribute-decorated property and method declarations (`/** @var ... */ #[Attr] public Type $name;`) no longer trigger the local-assertion heuristic; `naming.parameter-type-name` `shortTypeName` learned `UnionType` with an IntersectionType guard so `Foo|null` / `null|Foo` are unwrapped to `Foo` (matching `?Foo`) while `Foo|Bar` (true union) and `(Stringable&Countable)|null` (intersection arm) stay silent. **Phase 2 (policy lock):** `docs.missing-return-tag` now carries an explicit `$description` and fixture coverage for `void` / `never` methods with partial docblocks so a future agent cannot silently re-narrow the rule back to "void methods skipped"; cites `.goat-flow/lessons/workflow.md` "Respect explicit rule style" as the policy anchor. **Phase 3 (calibration):** `naming.parameter-type-name` gains an `ignoredParameterNames` option (default `[]`); gruff's own `.gruff.yaml` opts in to the AST-walker convention list `['unit', 'context', 'node', 'stmt', 'expr']`, dropping the rule's project-wide finding count from 549 to 259 on `src/`. The dogfood list is **project-specific** — downstream projects should review and pick their own names rather than copying gruff's verbatim. `waste.redundant-variable` `$description` clarified to spell out the `[assign, return]`-only precondition (text-only; behavior unchanged). The `docs.return-comment` + `docs.continue-comment` default-severity downgrade is deferred to `.goat-flow/tasks/0.2/M02-control-flow-comment-severity.md`. (`src/Rule/Docs/VarAnnotationDescriptionRule.php`, `src/Rule/Docs/MissingReturnTagRule.php`, `src/Rule/Naming/ParameterTypeNameRule.php`, `src/Rule/Waste/RedundantVariableRule.php`, `tests/Fixtures/Docs/var-annotation-description.php`, `tests/Fixtures/Docs/phpdoc-tags.php`, `tests/Fixtures/Naming/parameter-type-name.php`, `tests/Rule/Docs/DocsRulesTest.php`, `tests/Rule/Naming/NamingRulesTest.php`, `.gruff.yaml`)
- **Post-review hardening (M30)** - Hardened the local dashboard scan endpoint against option injection, Host-header abuse, and oversized requests; validated Git diff/archive refs before subprocess execution; made baseline writes atomic and tightened file-IO permissions; fixed cyclomatic `match` arm counting, multiple-AAA double counting, inherited global-state cleanup, and high-entropy long hex/alpha detection; expanded CI to PHP 8.3/8.4; and replaced Composer's hand-enumerated PHP syntax checks with a source-tree walk that skips the intentional syntax-error fixture. (`src/Command/Dashboard*`, `src/Diff/GitDiffProvider.php`, `src/Review/GitArchiveSnapshot.php`, `src/Baseline/BaselineStore.php`, `src/Trend/TrendRecorder.php`, `src/Rule/`, `.github/workflows/ci.yml`, `composer.json`, `tests/`)
- **PHPUnit config rules gated on test-file presence** - `test-quality.phpunit-deprecations-not-fatal`, `test-quality.phpunit-strict-flags-missing`, and `test-quality.phpunit-coverage-source-missing` previously fired once per project root regardless of which file was being scanned. Targeting a single production file (`gruff analyse src/Foo.php`) produced two-to-three `phpunit.xml.dist:1` warnings that had nothing to do with the analysed file. The rules now require at least one PHPUnit-test-shaped unit in scope before emitting — detected via a new `TestQualityNodeHelper::looksLikePhpUnitTestFile()` path check (`/tests/`, `/Tests/`, basename ending `Test.php`/`TestCase.php`). Project-wide scans (`gruff analyse src tests`, or `.`) still fire the rules unchanged because their test-file scope is non-empty. (`src/Rule/TestQuality/TestQualityNodeHelper.php`, `src/Rule/TestQuality/PhpUnitDeprecationsNotFatalRule.php`, `src/Rule/TestQuality/PhpUnitStrictFlagsMissingRule.php`, `src/Rule/TestQuality/PhpUnitCoverageSourceMissingRule.php`)
- **Lockfile filenames default-ignored** - `SourceDiscovery` now adds well-known lockfile filenames (`bun.lockb`, `composer.lock`, `npm-shrinkwrap.json`, `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`) to its default ignore list. Previously `package-lock.json` and similar `.json`/`.yaml` lockfiles were classified as text units and scanned by `SourceTextRuleInterface` rules, generating thousands of `sensitive-data.high-entropy-string` false positives on npm integrity hashes (observed at 1441 hits on healthkit's `feat/64272_voice-olb` branch diff). The new `IGNORED_FILENAMES` constant in `SourceDiscovery::isDefaultIgnoredPath()` skips lockfiles at every discovery path (root or nested), respecting `--include-ignored` for explicit opt-in. (`src/Source/SourceDiscovery.php`, `tests/Source/SourceDiscoveryTest.php`, `tests/Fixtures/Source/mixed/package-lock.json`, `tests/Fixtures/Source/mixed/composer.lock`)
- **PHPDoc and interface rules (M31)** - Added six advisory rules surfaced by the healthkit `feat/64272_voice-olb` PR review. `modernisation.phpdoc-mixed-overuse` flags `mixed` in PHPDoc tag types (`@param`, `@return`, `@var`, `@property`, `@phpstan-*`, `@psalm-*`) while skipping standalone PHPDoc-only `mixed` already reported by the signature rule. Four `docs.missing-*-phpdoc` rules - class, file, property (declared and constructor-promoted with `@param` pairing), and constant (with enum-level-docblock exemption) - share an AST-walker pattern with the existing `MissingPublicPhpdocRule`. `design.single-implementor-interface` runs as the first `ProjectRuleInterface` (new project-level rule seam, see ADR-003) and flags internal interfaces with exactly one implementor and no external type-hint usage, while exempting interfaces that extend PSR/Symfony/Doctrine/Twig/League/PhpParser/PHPUnit contracts or carry Symfony/Doctrine framework attributes. (`src/Rule/Modernisation/PhpDocMixedOveruseRule.php`, `src/Rule/Docs/MissingClassPhpdocRule.php`, `src/Rule/Docs/MissingFilePhpdocRule.php`, `src/Rule/Docs/MissingPropertyPhpdocRule.php`, `src/Rule/Docs/MissingConstantPhpdocRule.php`, `src/Rule/Design/SingleImplementorInterfaceRule.php`, `src/Rule/ProjectRuleInterface.php`, `src/Rule/RuleRegistry.php`, `.goat-flow/decisions/ADR-003-project-rule-seam.md`, `tests/Fixtures/Modernisation/phpdoc-mixed-overuse.php`, `tests/Fixtures/Docs/missing-{class,file,property,constant}-phpdoc*`, `tests/Fixtures/Design/single-implementor-interface/`, `tests/Rule/Modernisation/PhpDocMixedOveruseRuleTest.php`, `tests/Rule/Docs/DocsRulesTest.php`, `tests/Rule/Design/SingleImplementorInterfaceRuleTest.php`, `.gruff.yaml`)
- **Agent review workflow and rule discovery** - Added `list-rules` table/JSON output generated from `RuleRegistry::defaults()`, branch-review mode via `analyse --diff-vs=<base> [--changed-only]` with introduced/removed/unchanged findings compared by stable review identity, display filters (`--min-severity`, pillar/rule include/exclude flags), SARIF output, and Markdown grouping suitable for PR comments. JSON reports now include `run.filters`, optional `review`, and empty finding metadata as `{}`. (`src/Command/ListRulesCommand.php`, `src/Command/AnalyseCommand.php`, `src/Review/`, `src/Reporting/`, `tests/Review/AgentWorkflowCliTest.php`, `docs/gruff-cli-agent-instructions.md`)
- **Identifier and trivial wrapper rules** - Added `naming.identifier-quality` for high-signal placeholder/generic/numbered identifiers with configurable vocabularies and metadata, plus `waste.one-line-method` for trivial one-line call-wrapper methods. The root `.gruff.yaml` lists both defaults with plain-English reasons. (`src/Rule/Naming/IdentifierQualityRule.php`, `src/Rule/Naming/IdentifierTokenizer.php`, `src/Rule/Waste/OneLineMethodRule.php`, `tests/Rule/Naming/NamingRulesTest.php`, `tests/Rule/Waste/WasteRulesTest.php`, `.gruff.yaml`)
- **Review-noise tightening** - Narrowed dynamic-call security findings for typed callable/Closure invocations, reduced env-style secret false positives by requiring value-side secret evidence, made PHPDoc rules override/inherit-doc aware, kept array-shape/PHPStan PHPDoc from being called useless, and marks capped NPath values explicitly in messages and metadata. (`src/Rule/Security/DangerousFunctionCallRule.php`, `src/Rule/SensitiveData/HardcodedEnvValueRule.php`, `src/Rule/Docs/`, `src/Rule/Complexity/NpathComplexityRule.php`, `tests/Rule/`)
- **YAML-only config files** - `ConfigLoader` now accepts only `.yaml` and `.yml` config extensions; any other extension raises a `ConfigException`. The previous JSON decode path was removed and the `--config` help text on `analyse`, `report`, and `dashboard` was tightened accordingly. JSON config fixtures under `tests/Fixtures/Config/` were converted to YAML and the `ConfigLoaderTest` `writeTempConfig()` helper now defaults to a `.yaml` suffix. (`src/Config/ConfigLoader.php`, `src/Command/AnalyseCommand.php`, `src/Command/ReportCommand.php`, `src/Command/DashboardCommand.php`, `tests/Fixtures/Config/*.yaml`, `tests/Config/ConfigLoaderTest.php`, `README.md`)
- **Package scaffold and verification gates** - Bootstrapped `gruff-php` as a Composer CLI package with the `bin/gruff` entry point, Symfony Console wiring, PHPUnit coverage, PHPStan level 10, and a readable preflight runner for static analysis plus tests. (`composer.json`, `composer.lock`, `bin/gruff`, `src/Console/Application.php`, `phpunit.xml.dist`, `phpstan.neon.dist`, `scripts/preflight-checks.sh`)
- **Source discovery and parser pipeline** - Added deterministic PHP file discovery with default ignored directories, missing/ignored path reporting, per-file parser diagnostics, token and AST capture, and parent-node attributes for rules that need enclosing class context. Parse errors are reported without aborting the whole run. (`src/Source/`, `src/Parser/`, `tests/Source/SourceDiscoveryTest.php`, `tests/Parser/PhpFileParserTest.php`, `tests/Fixtures/Source/`)
- **Rule engine, finding schema, and config** - Rules now share a `RuleInterface`, stable `RuleDefinition` metadata, default thresholds, enable/disable support, and deterministic registry execution. Findings carry rule ID, message, file, location, severity, pillar, tier, confidence, remediation, fingerprint, and metadata. Project config supports per-rule threshold overrides, concise `threshold` + `severity` settings for warning/error metric rules, and strict unknown-key validation. (`src/Rule/`, `src/Finding/`, `src/Config/`, `tests/Rule/RuleRegistryTest.php`, `tests/Finding/FindingTest.php`, `tests/Config/ConfigLoaderTest.php`)
- **CLI reporting and exit codes** - `analyse` now emits grouped text reports or schema-versioned JSON (`gruff.analysis.v1`) with tool metadata, run config, summary counts, diagnostics, ignored/missing paths, and findings. Exit codes are `0` for clean or below-threshold runs, `1` for findings meeting `--fail-on`, and `2` for expected config, parse, or input errors. (`src/Command/AnalyseCommand.php`, `src/Analysis/`, `src/Reporting/`, `tests/Console/GruffCliTest.php`, `tests/Fixtures/Cli/`)
- **Size metrics** - Added seven threshold-backed size checks for file length, class/trait/enum length, method/function/closure length, parameter count, public method count, property count, and average method length per class. Counts use PhpParser source spans and dual warn/error thresholds. (`src/Rule/Size/`, `tests/Fixtures/Size/`)
- **Complexity metrics** - Added six structural complexity checks: cyclomatic complexity, cognitive complexity, maximum nesting depth, NPath complexity, Halstead volume, and maintainability index. Algorithms cover boolean operators, ternaries, nesting penalties, path multiplication, and inverse thresholds where lower scores are worse. (`src/Rule/Complexity/`, `tests/Fixtures/Complexity/`)
- **Dead code and waste rules** - Added class-local unused private method/property detection plus waste checks for unreachable statements, empty methods/classes, unused imports, unused parameters, and commented-out code. The scope stays conservative to avoid framework and inheritance false positives. (`src/Rule/DeadCode/`, `src/Rule/Waste/`, `tests/Fixtures/DeadCode/`)
- **Naming rules** - Added naming checks for generic method names, single-character variables, boolean prefix conventions, Hungarian notation, vague standalone class names, mixed test naming conventions, and class/file mismatches. (`src/Rule/Naming/`, `tests/Fixtures/Naming/`)
- **Documentation rules** - Added documentation quality checks for missing public PHPDoc, missing `@param` / `@return` / `@throws` tags, stale `@param` tags, useless docblocks, TODO/FIXME density, and missing project README files. (`src/Rule/Docs/`, `tests/Rule/Docs/DocsRulesTest.php`, `tests/Fixtures/Docs/`)
- **Security dangerous-pattern rules** - Added heuristic AST rules for dangerous execution calls, unsafe `unserialize()`, weak crypto, variable include/require paths, SQL string concatenation, request-controlled `header()` calls, error suppression, silent catches, `extract()`/`compact()` on request data, insecure random sources, and disabled cURL SSL verification. Findings are labelled as pattern or heuristic checks and covered by safe-wrapper, config-disable, cumulative, and CLI fixtures. (`src/Rule/Security/`, `tests/Rule/Security/SecurityRulesTest.php`, `tests/Fixtures/Security/`)
- **Secrets and sensitive-data rules** - Added source-tree text scanning for PHP and config-like files, with redacted findings for AWS keys, private key headers, common API keys, JWTs, database URLs with inline passwords, env-style secret values, high-entropy strings, PHI identifiers, and realistic PII in test fixtures. Non-PHP text files now bypass PHP parsing and only run text-safe rules; redaction tests assert full synthetic secrets stay out of text and JSON reports. (`src/Rule/SensitiveData/`, `src/Rule/SourceTextRuleInterface.php`, `src/Source/`, `tests/Rule/SensitiveData/SensitiveDataRulesTest.php`, `tests/Fixtures/SensitiveData/`)
- **PHP modernisation rules** - Added version-gated opportunity checks for constructor property promotion, readonly properties, public mutable properties, enum candidates, match-expression candidates, first-class callable arrays, named-argument opportunities, mixed-type overuse, and direct superglobal access. `minimumPhpVersion` config gates PHP 8.0/8.1 suggestions, and `@` suppression now reports once as a security finding with Modernisation as a secondary pillar. (`src/Rule/Modernisation/`, `src/Config/`, `src/Rule/Security/ErrorSuppressionRule.php`, `tests/Rule/Modernisation/ModernisationRulesTest.php`, `tests/Fixtures/Modernisation/`)
- **Static test-quality rules** - Added PHPUnit/Pest scoped test-smell checks for no assertions, trivial assertions, conditional and looped tests, long tests with a single apparent SUT call, eager tests, mystery guests, excessive mocking, mock-only tests, sleeps, naming inconsistency, magic-number assertions, private reflection, `@dataProvider` annotations, trivial snapshots, missing SUT calls, setup bloat, and skipped tests without reasons. (`src/Rule/TestQuality/`, `tests/Rule/TestQuality/TestQualityRulesTest.php`, `tests/Fixtures/TestQuality/`)
- **Infection mutation ingestion** - Added optional Infection JSON report ingestion via `--infection-report`, explicit opt-in execution via `--infection-run`, per-file MSI / covered MSI summaries, escaped and timed-out mutant findings, mutation budget checks, and baseline MSI regression checks. Missing Infection binaries and malformed reports become clear run diagnostics without making Infection a mandatory dependency. (`src/Mutation/`, `src/Command/AnalyseCommand.php`, `src/Reporting/TextReporter.php`, `tests/Mutation/InfectionReportParserTest.php`, `tests/Fixtures/Mutation/Infection/`)
- **Scoring, HTML report, and diff outputs** - Added A-F composite and per-pillar scoring, design composite findings for oversized complex methods, Git diff-mode filtering for changed lines/files, self-contained HTML report output, Markdown PR-comment output, GitHub Actions annotations, hotspot-map JSON, and optional trend-history recording via `--history-file`. (`src/Scoring/`, `src/Diff/`, `src/Trend/`, `src/Reporting/`, `tests/Scoring/ScoreCalculatorTest.php`, `tests/Diff/GitDiffProviderTest.php`, `tests/Reporting/HtmlReporterTest.php`)
- **Config selection and auditable baselines** - Extended project config with project-relative path ignores, accepted abbreviation and secret-preview allowlists, and explicit rule selection by tier, pillar, and rule id. Added `--baseline` and `--generate-baseline` for fingerprint-backed suppression with stale-entry reporting, leaving inline suppression comments out of v0.1. (`src/Config/`, `src/Baseline/`, `src/Source/SourceDiscovery.php`, `src/Command/AnalyseCommand.php`, `tests/Config/ConfigLoaderTest.php`, `tests/Console/GruffCliTest.php`, `tests/Fixtures/Config/`)
- **Dogfood threshold calibration** - Workspace dogfood runs now have recorded performance and finding calibration evidence. Noisy defaults were tightened without dropping v0.1 coverage: identical project-level findings are de-duplicated, maintainability-index defaults moved to `55/35`, named-argument opportunities require 5 positional args unless a positional boolean is present, PHPDoc checks avoid marker-only and trivial-method busywork, useless-PHPDoc detection keeps generic types and tag descriptions, promoted constructors and empty exception markers are exempt from dead-code findings, fixture PII skips author/copyright emails, high-entropy scanning skips path literals, and no-assertion tests recognize legacy exception annotations plus mock expectations. (`src/Rule/`, `tests/Rule/`, `tests/Fixtures/`)
- **Report and dashboard commands** - Added `gruff report` for static HTML/JSON report output and `gruff dashboard` for the local interactive server. The dashboard serves a control page with project root, paths, config, fail threshold, include-ignored controls, refreshes scans through `/scan`, and can point gruff at another local project without restarting. `scripts/start-dev.sh` starts the dashboard with environment-overridable host, port, project root, and scan timeout. Release smoke coverage now exercises command listing, clean-checkout help, static report output, JSON report output, report file writing, dashboard refresh, and cross-project dashboard scans. (`src/Command/ReportCommand.php`, `src/Command/DashboardCommand.php`, `src/Console/Application.php`, `scripts/start-dev.sh`, `tests/Console/GruffCliTest.php`)
- **Release verification workflow** - Added CI for push and pull request verification on PHP 8.3. The workflow installs Composer dependencies, runs `composer check`, then runs the preflight script so local and CI gates share the same PHPStan level 10 and PHPUnit checks. (`.github/workflows/ci.yml`, `composer.json`, `scripts/preflight-checks.sh`)
- **Performance and rule applicability fixes** - HTML report output is now ~40x faster: `analyse src --format html` dropped from ~99 s to ~2 s by writing reporter output via `OutputInterface::OUTPUT_RAW` so Symfony Console no longer scans the report body for `<tag>` formatting markers. The 18 test-quality rules share one cached AST walk per analysis unit (`TestQualityNodeHelper::testScopes` is memoised by `WeakMap`), and bare `test*` method names only match tests when their enclosing class extends a `*TestCase` base, removing false positives that previously fired on library code. `MissingReadmeRule` short-circuits after its first emission and caches its filesystem stat per project root. The cleanup batch removes five unused imports from rule classes and stops `UselessPhpdocRule` from flagging `@param resource` (PHP cannot express `resource` as a signature type). (`src/Command/AnalyseCommand.php`, `src/Command/ReportCommand.php`, `src/Rule/RuleRegistry.php`, `src/Rule/Docs/MissingReadmeRule.php`, `src/Rule/Docs/UselessPhpdocRule.php`, `src/Rule/TestQuality/TestQualityNodeHelper.php`, `tests/Fixtures/TestQuality/`, `tests/Rule/TestQuality/TestQualityRulesTest.php`)
- **Baseline workflow ergonomics** - `gruff analyse --generate-baseline` without an argument writes to `gruff-baseline.json` at the project root, overwrites silently, and prints an actionable tip in text output. Subsequent `gruff analyse` runs auto-discover the same file and apply it; `--no-baseline` opts a single run out, `--baseline=path.json` forces an explicit file, and `--baseline` plus `--no-baseline` is rejected as a usage error. `gruff report` and `gruff dashboard` forward the same apply flags to the underlying analyser, and the baseline payload now includes a `source` field that distinguishes auto-discovered (`default`) from explicit (`explicit`) application. New finding visibility, stale-entry reporting, and invalid-schema diagnostics retain their existing semantics. (`src/Command/AnalyseCommand.php`, `src/Command/ReportCommand.php`, `src/Command/DashboardCommand.php`, `src/Baseline/BaselineStore.php`, `src/Baseline/BaselineReport.php`, `src/Reporting/TextReporter.php`, `tests/Console/GruffCliTest.php`)
- **Self-analysis noise reduction** - Source discovery now prunes ignored subtrees with a `RecursiveCallbackFilterIterator` instead of descending and reporting every subdirectory, so a typical full-project run prints one ignored entry per root rather than hundreds (e.g. `.git/objects/00..ff`). The default ignored list now also covers `.fleet`, `.idea`, `.vscode`, `tmp`, and `.goat-flow/logs`, `.goat-flow/scratchpad`, and `.goat-flow/tasks`. The `sensitive-data.high-entropy-string` rule no longer flags pure-letter identifier strings (e.g. PascalCase tokens in IDE inspection profile XML). The `dead-code.unused-private-property` rule now also recognises `self::$prop`, `static::$prop`, and `OwnClass::$prop` reads/writes, so static private properties accessed only from within their declaring class are detected correctly. New CLI flag `--no-config` (mirrors `--no-baseline`) opts a single run out of auto-loading the project's default config. (`src/Source/SourceDiscovery.php`, `src/Rule/SensitiveData/HighEntropyStringRule.php`, `src/Rule/DeadCode/UnusedPrivatePropertyRule.php`, `src/Command/AnalyseCommand.php`, `src/Command/ReportCommand.php`, `src/Command/DashboardCommand.php`, `.gruff.yaml`)
- **Sensitive-data taxonomy rename** - The pre-v0.1 `Secrets` pillar and `secrets.*` rule IDs were renamed to `SensitiveData` and `sensitive-data.*`, aligning the public taxonomy with the directory rename to `src/Rule/SensitiveData/` (namespace `GruffPhp\Rule\SensitiveData`). The test class moves to `tests/Rule/SensitiveData/SensitiveDataRulesTest.php`. Public report output now serializes the pillar as `sensitive-data`. The internal `allowlists.secretPreviews` config key and `AnalyseCommand::filterAllowedSecretPreviews()` method names are intentionally preserved to avoid silent config breakage. (`src/Finding/Pillar.php`, `src/Rule/SensitiveData/`, `src/Scoring/ScoreCalculator.php`, `src/Command/AnalyseCommand.php`, `src/Rule/RuleRegistry.php`, `tests/Rule/SensitiveData/SensitiveDataRulesTest.php`, `tests/Rule/RuleRegistryTest.php`, `tests/Fixtures/Cli/Golden/`, `tests/Fixtures/Config/disable-high-entropy.yaml`, `tests/Console/GruffCliTest.php`, `composer.json`)
- **Dashboard mutation runner** - The HTML report's chart row now always renders both the cyclomatic-complexity card and a mutation card (previously the mutation card collapsed to `chart-grid-solo` when no Infection report was supplied, making the second graph slot vanish). When the report is rendered inside `gruff dashboard` and mutation data is absent, both the mutation pillar card and the mutation chart card grow a "Run mutation analysis" link styled as a button that submits to `/scan?mutation=run`; the dashboard server adds `--infection-run --infection-report infection-report.json` to the analyse subprocess. Standalone `gruff report --format html` and `gruff analyse --format html` runs still show the CLI hint and never the button. Mutation-related run diagnostics (`mutation-tool-error`, `mutation-run-error`, `mutation-report-error`) are now surfaced inline in the empty mutation cards so missing `infection` binaries, malformed JSON reports, or non-zero Infection exits are visible from the dashboard rather than silently producing a fake-green pillar grade. (`src/Reporting/HtmlReporter.php`, `src/Command/DashboardCommand.php`, `tests/Reporting/HtmlReporterTest.php`, `tests/Console/GruffCliTest.php`)
- **HTML report clarity and interactive findings** - The HTML report now uses one severity vocabulary (`errors`, `warnings`, `advisories`), stops colouring advisory counts green, promotes report sections and finding rule labels to real headings, adds scoped table headers, replaces the generic verdict subtitle with finding-count copy, and adds a cyclomatic-complexity summary above the histogram. File locations are selectable copy spans by default, with opt-in `--report-editor-link=vscode|phpstorm` links for `analyse` and `report`. New opt-in `--report-interactive` adds inline, dependency-free severity/pillar/path/text filters, file/rule grouping, live counts, and URL-hash state to the findings list; dashboard scans expose the same toggle and the dashboard scan command is now wrapping/copyable. (`src/Reporting/HtmlReporter.php`, `src/Command/AnalyseCommand.php`, `src/Command/ReportCommand.php`, `src/Command/DashboardPageRenderer.php`, `src/Command/DashboardScanCommandBuilder.php`, `tests/Reporting/HtmlReporterTest.php`, `tests/Console/GruffCliTest.php`, `tests/Fixtures/Reporting/InteractiveReport/`)
- **Dashboard scan controls** - The dashboard controls panel now has a wider, denser layout with readable scan metadata, aligned checkbox options, an explicit `.gruff.yaml` config-path default, and a scan-scope selector for whole-branch scans versus diff-only scans backed by `analyse --diff`. The root `.gruff.yaml` now records the default config surface explicitly without pinning a project-root path, uses single `threshold` + `severity` entries for metric rules, and explicit `.json` config paths remain readable. (`.gruff.yaml`, `src/Command/DashboardCommand.php`, `src/Command/DashboardPageRenderer.php`, `src/Command/DashboardStateFactory.php`, `src/Command/DashboardScanCommandBuilder.php`, `src/Config/RuleConfigApplier.php`, `tests/Config/ConfigLoaderTest.php`, `tests/Console/GruffCliTest.php`)
