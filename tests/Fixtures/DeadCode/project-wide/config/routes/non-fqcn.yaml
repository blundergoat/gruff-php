service_id_controller:
  path: /service-id
  defaults:
    _controller: app.controller.service_id::index

legacy_controller:
  path: /legacy
  defaults: { _controller: AppBundle:Legacy:index }

other_key_controller:
  path: /other-key
  defaults:
    not_controller: App\Controller\OtherKeyController::index
