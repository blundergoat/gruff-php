---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Tick task checkboxes the moment the item is done, never in a batch at the end](discipline.md) (search: "## Lesson: Tick task checkboxes the moment the item is done, never in a batch at the end") - Working a `.goat-flow/plans/` milestone (or any file with `- [ ]` items) across multiple steps.
- [Fresh scaffold still needs real learning-loop entries for stats](setup.md) (search: "## Lesson: Fresh scaffold still needs real learning-loop entries for stats") - During initial goat-flow setup, `node --import tsx src/cli/cli.ts stats /home/devgoat/projects/gruff-workspace/gruff-php --check` failed because `.goat-flow/learning-loop/footguns/` and…
- [Fan-out author agents fabricate rationale in generated comments — a mechanical gate proves shape, not accuracy](workflow.md) (search: "## Lesson: Fan-out author agents fabricate rationale in generated comments — a mechanical gate proves shape, not accuracy") - To rewrite every comment/docblock in a 50-file `src/` batch to the UI-focused style spec, the agent ran a dynamic Workflow as an author→verify pipeline (one author agent per file restores the clean…
- [Comment codemods must not insert null-check comments after terminating statements inside closures](workflow.md) (search: "## Lesson: Comment codemods must not insert null-check comments after terminating statements inside closures") - During the literal source comment sweep, the token codemod saw an outer `!== null` check after a `findFirst(..., static function (...) { return ...; })` call and inserted a `// User view:` line…
- [Don't `git stash` to probe pre-existing state — it's not read-only and can entangle other stashes](workflow.md) (search: "## Lesson: Don't `git stash` to probe pre-existing state — it's not read-only and can entangle other stashes") - While verifying whether three `docs.stale-param-tag` warnings from the dogfood `analyse` were pre-existing or introduced this session, the agent ran `git stash push --keep-index --include-untracked…
- [Universal defaults that ship via `init` must also flow through `AnalysisConfig` so existing projects benefit](workflow.md) (search: "## Lesson: Universal defaults that ship via `init` must also flow through `AnalysisConfig` so existing projects benefit") - `InitCommand::DEFAULT_ACCEPTED_ABBREVIATIONS` was added in an earlier WIP commit so freshly-scaffolded `.gruff-php.yaml` files would seed the 16 universal abbreviations (`age, app, db, fs, id, io,…
- [When the dogfood config has tuned a rule, those tunings are the real defaults](workflow.md) (search: "## Lesson: When the dogfood config has tuned a rule, those tunings are the real defaults") - `waste.one-line-method` shipped with `minInFileCallers: 0` and `namedAlternativeFactoryExempt: false` as `defaultOptions`.
- [Keyword-based exemptions need fixture audits because existing positive-case fixtures often use the trigger words](workflow.md) (search: "## Lesson: Keyword-based exemptions need fixture audits because existing positive-case fixtures often use the trigger words") - Added a docblock-keyword exemption to `docs.regex-comment` per M02 spec: when the enclosing function-like's docblock contains any of `regex`, `pattern`, `preg_`, `match`, or the call's own function…
- [Validator throws need an actionable hint, and the CLI catch must render it](workflow.md) (search: "## Lesson: Validator throws need an actionable hint, and the CLI catch must render it") - `ConfigLoader` throws `ConfigException` (extends `RuntimeException`) at every config-validation boundary — schemaVersion missing/mismatched, unknown rule id, malformed `minimumSeverity`, etc.
- [When introducing a required config field, auto-inject in the test helper rather than patching every fixture](workflow.md) (search: "## Lesson: When introducing a required config field, auto-inject in the test helper rather than patching every fixture") - M11 introduced `schemaVersion: gruff-php.config.v0.1` as a required top-level field in `.gruff-php.yaml`, with `ConfigLoader::assertSchemaVersion` hard-erroring on missing/wrong values.
- [Never replace hand-maintained project config with generated defaults](workflow.md) (search: "## Lesson: Never replace hand-maintained project config with generated defaults") - The project `.gruff-php.yaml` was regenerated from `gruff-php init` defaults during rule-default work.
- [Fix generators when generated config is wrong](workflow.md) (search: "## Lesson: Fix generators when generated config is wrong") - After the project ignore list was restored in `.gruff-php.yaml`, the underlying `gruff-php init` generator still emitted a config that could recreate the same loss.
- [Never run `git commit` or `git push`](workflow.md) (search: "## Lesson: Never run `git commit` or `git push`") - After the user authorised "verify and commit as 2 focused commits" via an in-conversation question, the agent ran `git commit -F <file>`.
- [Tick the per-task checkboxes when flipping a plan to complete](workflow.md) (search: "## Lesson: Tick the per-task checkboxes when flipping a plan to complete") - Three milestone plans (`.goat-flow/plans/_archive/0.1/M19-performance-and-rule-quick-wins.md`, `M20-phpstan-style-baseline-workflow.md`, `M21-sensitive-data-rename.md`) were marked `Status: complete`…
- [Reconcile shipped work back into task checkboxes immediately](workflow.md) (search: "## Lesson: Reconcile shipped work back into task checkboxes immediately") - Several v0.1 milestone files (`.goat-flow/plans/_archive/0.1/M54-pest-expectation-allowlist-expansion.md`, `M56-size-rubric-metric-semantics.md`, `M57-statement-dispatch-visitor-consolidation.md`,…
- [Adding a rule cascades through fixtures, goldens, and existing tests](workflow.md) (search: "## Lesson: Adding a rule cascades through fixtures, goldens, and existing tests") - M31 added six new rules: `modernisation.phpdoc-mixed-overuse` (phase 1) and four `docs.missing-*-phpdoc` rules plus `design.single-implementor-interface` (phases 2 + 3).
- [Respect explicit rule style even when it restates native syntax](workflow.md) (search: "## Lesson: Respect explicit rule style even when it restates native syntax") - During external dogfooding, the user asked why private helper docblocks without `@return` were not being flagged.
- [Reviewer "outside diff" coverage is not exhaustive — sweep the whole repo before reporting scope](workflow.md) (search: "## Lesson: Reviewer") - Reviewing PR #6 (the `naming.parameter-type-name` retirement), CodeRabbit's "outside diff range" comments flagged two stale rule-count references:
