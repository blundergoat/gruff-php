---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Split scaffold-via-`Yaml::dump` and rules-via-manual-string when init output needs per-rule comments](commands.md) (search: "## Pattern: Split scaffold-via-`Yaml::dump` and rules-via-manual-string when init output needs per-rule comments") - The `init` command originally built one PHP array (paths + allowlists + selection + rules) and…
- [Symfony Console execute() shape for commands that own side effects](commands.md) (search: "## Pattern: Symfony Console execute() shape for commands that own side effects") - Several gruff CLI commands accept user-supplied flags that can be malformed (`--fail-on bogus`,…
- [Carry an actionable hint on every user-input exception](error-handling.md) (search: "## Pattern: Carry an actionable hint on every user-input exception") - User-facing CLI commands (`analyse`, `summary`, `report`, `dashboard`, `init`) validate config and…
- [Path-limit snapshots before materialising them](performance.md) (search: "## Pattern: Path-limit snapshots before materialising them") - Branch-review analysis can compare current findings against a Git base snapshot.
- [Cache repeated AST metrics within one analyse process](performance.md) (search: "## Pattern: Cache repeated AST metrics within one analyse process") - `scripts/test-performance.sh --full --corpus=all` identified repeated AST traversals as a real…
- [Degrade deep source analysis and bind its performance baseline](performance.md) (search: "## Pattern: Degrade deep source analysis and bind its performance baseline") - Oversized PHP source still needs raw-text security and size analysis even when materialising a PHP…
- [Remediation text shape — fix sentence plus config-hatch sentence](rules.md) (search: "## Pattern: Remediation text shape — fix sentence plus config-hatch sentence") - Every rule has an escape hatch in `defaultOptions` for the cases where the rule's heuristic…
- [Project-level rule seam](rules.md) (search: "## Pattern: Project-level rule seam") - Most gruff rules implement `RuleInterface` and see one `AnalysisUnit` at a time, which is…
- [Intersection-typed test fake for stream routing](tests.md) (search: "## Pattern: Intersection-typed test fake for stream routing") - Some console code branches on `$output instanceof ConsoleOutputInterface` to route prompts or…
- [Prove a comment-only edit with a whitespace-insensitive diff against the pre-edit baseline](verification.md) (search: "## Pattern: Prove a comment-only edit with a whitespace-insensitive diff against the pre-edit baseline") - When rewriting comments/docblocks across many `src/` files, each edit must be proven to change…
