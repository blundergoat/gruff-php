---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Side-effect prompts placed before forwarded-option validation mutate the project on bad inputs](commands.md) (search: "## Footgun: Side-effect prompts placed before forwarded-option validation mutate the project on bad inputs") - `src/Cli/Command/ReportCommand.php` (search: `MissingConfigPrompt::maybeOffer`) validates its own…
- [Finding-scope filters must treat an empty target set as drop-all, not pass-through](commands.md) (search: "## Footgun: Finding-scope filters must treat an empty target set as drop-all, not pass-through") - `src/Cli/Command/AnalysisFindingSupport.php` holds sibling finding filters with deliberately…
- [The result cache keys on tool version + config + rule set, not rule source, so a rule-LOGIC change does not invalidate cached findings](commands.md) (search: "## Footgun: The result cache keys on tool version + config + rule set, not rule source, so a rule-LOGIC change does not invalidate cached findings") - `src/Engine/Cache/AnalysisFingerprint.php` (search: `forRun`) builds the per-file cache key from…
- [Ignored scans can exit zero: `--include-ignored` bypasses defaults, not configured `paths.ignore`](commands.md) (search: "## Footgun: Ignored scans can exit zero: `--include-ignored` bypasses defaults, not configured `paths.ignore`") - `src/Engine/Source/PathIgnoreResolver.php` (search: `Configured paths.ignore wins first and…
- [A score withheld as inapplicable still reaches every value derived from it](commands.md) (search: "## Footgun: A score withheld as inapplicable still reaches every value derived from it") - `ScoreCalculator` scores an empty finding set as 100, so a run that discovers no files produces a…
- [Hook file-discovery and changed-range derivation must cover the same git states](hooks.md) (search: "## Footgun: Hook file-discovery and changed-range derivation must cover the same git states") - The gruff-code-quality hook discovers candidate files and then derives changed-line ranges for each…
- [Statement-type additions must extend `StmtChildVisitor`, not individual rules](rules.md) (search: "## Footgun: Statement-type additions must extend `StmtChildVisitor`, not individual rules") - Four rules (`NestingDepthRule`, `CognitiveComplexityRule`, `RedundantVariableRule`,…
- [Heuristic rules overmatch nested syntax shapes](rules.md) (search: "## Footgun: Heuristic rules overmatch nested syntax shapes") - Rule heuristics that search a whole docblock or AST subtree can attribute nested syntax to the…
- [Legacy rule IDs can outlive rule semantics](rules.md) (search: "## Footgun: Legacy rule IDs can outlive rule semantics") - `src/Rules/Docs/MissingPublicPhpdocRule.php` (search: `docs.missing-public-phpdoc`) keeps the…
- [Conservative member scopes can hide concrete public waste](rules.md) (search: "## Footgun: Conservative member scopes can hide concrete public waste") - `src/Rules/Waste/UnusedParameterRule.php` (search: `analysableNodes`) originally checked standalone…
- [Constructor-promoted properties bypass property-node scans](rules.md) (search: "## Footgun: Constructor-promoted properties bypass property-node scans") - Constructor-promoted properties are represented as `Node\Param` entries with visibility flags, not…
- [naming.boolean-prefix requires the prefix at the start of the identifier](rules.md) (search: "## Footgun: naming.boolean-prefix requires the prefix at the start of the identifier") - `src/Rules/Naming/BooleanPrefixRule.php` (search: `allowedPrefixes`) checks that bool-returning…
- [Retiring a rule leaves stale count references in five doc artefacts](rules.md) (search: "## Footgun: Retiring a rule leaves stale count references in five doc artefacts") - The rule registry's true count lives only in `src/Rules/RuleRegistry.php` (the…
- [PHPStan/Psalm array-shape exemptions need a "concrete sibling" gate, not "any nested mixed"](rules.md) (search: "## Footgun: PHPStan/Psalm array-shape exemptions need a") - `src/Rules/Modernisation/PhpDocMixedOveruseRule.php` (search: `isPreciseArrayShape`) exempts…
- [PHPStan rejects prose attached to multiline array-shape tags](rules.md) (search: "## Footgun: PHPStan rejects prose attached to multiline array-shape tags") - `docs.return-comment` only needs to know whether an `@return` tag has prose after the type, and…
- [Member-existence rules must honour PHP's split case rules — methods case-insensitive, properties case-sensitive](rules.md) (search: "## Footgun: Member-existence rules must honour PHP's split case rules — methods case-insensitive, properties case-sensitive") - PHP resolves method names case-insensitively but property names case-sensitively:…
- [`NodeIndex` enumerates declarations nested in functions and conditionals, not just top-level ones](rules.md) (search: "## Footgun: `NodeIndex` enumerates declarations nested in functions and conditionals, not just top-level ones") - `NodeIndex::nodesOfAny`/`nodesOf` return matches from a full preorder walk of the whole unit —…
- [Same-class callable-array detection must include `[__CLASS__, 'method']`, not just `$this`/`self::class`/`static::class`/`Class::class`](rules.md) (search: "## Footgun: Same-class callable-array detection must include `[__CLASS__, 'method']`, not just `$this`/`self::class`/`static::class`/`Class::class`") - `src/Rules/DeadCode/UnusedPrivateMethodRule.php` (search: `sameClassCallableArrayMethodName`)…
- [php-parser node-list properties type as `array<Stmt>` (int|string keys), so PHPStan L10 rejects an `array<int, Stmt>` parameter](rules.md) (search: "## Footgun: php-parser node-list properties type as `array<Stmt>` (int|string keys), so PHPStan L10 rejects an `array<int, Stmt>` parameter") - php-parser declares list properties such as `If_::$stmts`, `ClassMethod::$stmts`, and…
- ["Data carrier / value object" detection must classify behaviour by RETURN type, not by parameter presence](rules.md) (search: '## Footgun: "Data carrier / value object" detection must classify behaviour by RETURN type, not by parameter presence') - The size rules treat immutable value objects gently: `src/Rules/Size/PropertyCountRule.php`…
- [Size rules report zero, not an error, if they run after their unit is released](rules.md) (search: "## Footgun: Size rules report zero, not an error, if they run after their unit is released") - `src/Engine/Parser/AnalysisUnit.php` (search: `Break ParentConnectingVisitor's back-edges`) clears…
- [Shared serializers fan out into multiple versioned schemas](schemas.md) (search: "## Footgun: Shared serializers fan out into multiple versioned schemas") - `src/Results/Scoring/FileScore.php` (search: `public function toArray`) and…
- [Schema-version strings are stamped in many human-written places](schemas.md) (search: "## Footgun: Schema-version strings are stamped in many human-written places") - A `SCHEMA_VERSION` constant in PHP is just one of N stamps of the version string.
- [The `gruff-php.config.v0.1` literal lives in two source-of-truth places plus user-facing surfaces](schemas.md) (search: "## Footgun: The `gruff-php.config.v0.1` literal lives in two source-of-truth places plus user-facing surfaces") - ADR-015 introduced `schemaVersion: gruff-php.config.v0.1` as a required top-level key in…
- [goat-flow content audit requires a goat-flow-dashboard-views line in a consumer code map](setup.md) (search: "## Footgun: goat-flow content audit requires a goat-flow-dashboard-views line in a consumer code map") - goat-flow 1.14.0's `audit --check-content` runs `code-map-dashboard-view-drift` unconditionally on…
- [Package bin bootstraps must use Composer's consumer autoloader](setup.md) (search: "## Footgun: Package bin bootstraps must use Composer's consumer autoloader") - `bin/gruff-php` (search: `$GLOBALS['_composer_autoload_path']`) must prefer Composer's generated…
- [Consumer install tests can resolve newer dependency majors than the source checkout](setup.md) (search: "## Footgun: Consumer install tests can resolve newer dependency majors than the source checkout") - `composer.json` (search: `"symfony/console": "^6.4 || ^7.0 || ^8.0"`) allows Symfony Console 8 on…
- [`composer update --prefer-lowest` cannot reach the advertised floor when a dev dependency requires higher](setup.md) (search: "## Footgun: `composer update --prefer-lowest` cannot reach the advertised floor when a dev dependency requires higher") - `composer.json` (search: `"nikic/php-parser"`) advertises `^5.6`.
- [`goat-flow hooks sync` reverts this repo's local hook fixes; the failing drift audit is not a repair prompt](setup.md) (search: "## Footgun: `goat-flow hooks sync` reverts this repo's local hook fixes; the failing drift audit is not a repair prompt") - `goat-flow audit . --harness --agent claude` exits 1 with `hookCoverage` reporting…
- [Anonymous-class test fakes are scored by gruff's production rules](tests.md) (search: "## Footgun: Anonymous-class test fakes are scored by gruff's production rules") - The default `php bin/gruff-php analyse` scan invoked by `composer check` and…
- [The obvious data-provider consolidation trips phpdoc-mixed-overuse and the public-method cap](tests.md) (search: "## Footgun: The obvious data-provider consolidation trips phpdoc-mixed-overuse and the public-method cap") - `test-quality.repeated-structure-missing-data-provider`…
- [Two split-hex snapshot hashes lock the rule corpus and rule definitions; both break silently on fixture and defaultOptions changes](tests.md) (search: "## Footgun: Two split-hex snapshot hashes lock the rule corpus and rule definitions; both break silently on fixture and defaultOptions changes") - Two regression tests pin a SHA-256 over a serialized snapshot, and each is sensitive to a change a…
