---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Side-effect prompts placed before forwarded-option validation mutate the project on bad inputs](commands.md) (search: "## Footgun: Side-effect prompts placed before forwarded-option validation mutate the project on bad inputs") - `src/Cli/Command/ReportCommand.php` (search: `MissingConfigPrompt::maybeOffer`) validates its own `--output` directory before firing the init prompt, but the analyse-level options it forwards…
- [Finding-scope filters must treat an empty target set as drop-all, not pass-through](commands.md) (search: "## Footgun: Finding-scope filters must treat an empty target set as drop-all, not pass-through") - `src/Cli/Command/AnalysisFindingSupport.php` holds sibling finding filters with deliberately different — and easy-to-confuse — empty-set semantics.
- [The result cache keys on tool version + config + rule set, not rule source, so a rule-LOGIC change does not invalidate cached findings](commands.md) (search: "## Footgun: The result cache keys on tool version + config + rule set, not rule source, so a rule-LOGIC change does not invalidate cached findings") - `src/Engine/Cache/AnalysisFingerprint.php` (search: `forRun`) builds the per-file cache key from the resolved config, the enabled rule ids, and the gruff version (search: `toolVersion`, which writes…
- [`.goat-flow/scratchpad` (plus `/logs`, `/tasks`) is a default ignore, so scanning anything under it returns 0 files without `--include-ignored`](commands.md) (search: "## Footgun: `.goat-flow/scratchpad` (plus `/logs`, `/tasks`) is a default ignore, so scanning anything under it returns 0 files without `--include-ignored`") - `src/Engine/Source/PathIgnoreResolver.php` (search: `.goat-flow/scratchpad`) ships `.goat-flow/logs`, `.goat-flow/scratchpad`, and `.goat-flow/tasks` in the built-in default ignore set…
- [Hook file-discovery and changed-range derivation must cover the same git states](hooks.md) (search: "## Footgun: Hook file-discovery and changed-range derivation must cover the same git states") - The gruff-code-quality hook discovers candidate files and then derives changed-line ranges for each through two independent git queries that must agree on which working states count as "changed".
- [Statement-type additions must extend `StmtChildVisitor`, not individual rules](rules.md) (search: "## Footgun: Statement-type additions must extend `StmtChildVisitor`, not individual rules") - Five rules (`NestingDepthRule`, `NpathComplexityRule`, `CognitiveComplexityRule`, `RedundantVariableRule`, `UnreachableCodeRule`) share `src/Rules/Shared/StmtChildVisitor.php` (search: `childBlocks`)…
- [Heuristic rules overmatch nested syntax shapes](rules.md) (search: "## Footgun: Heuristic rules overmatch nested syntax shapes") - Rule heuristics that search a whole docblock or AST subtree can attribute nested syntax to the wrong owner.
- [Legacy rule IDs can outlive rule semantics](rules.md) (search: "## Footgun: Legacy rule IDs can outlive rule semantics") - `src/Rules/Docs/MissingPublicPhpdocRule.php` (search: `docs.missing-public-phpdoc`) keeps the historical rule ID for output compatibility, but the rule now requires local PHPDoc on every method…
- [Conservative member scopes can hide concrete public waste](rules.md) (search: "## Footgun: Conservative member scopes can hide concrete public waste") - `src/Rules/Waste/UnusedParameterRule.php` (search: `analysableNodes`) originally checked standalone functions and private methods only, so a concrete public method like an external healthcare…
- [Constructor-promoted properties bypass property-node scans](rules.md) (search: "## Footgun: Constructor-promoted properties bypass property-node scans") - Constructor-promoted properties are represented as `Node\Param` entries with visibility flags, not as `Stmt\Property` declarations.
- [naming.boolean-prefix requires the prefix at the start of the identifier](rules.md) (search: "## Footgun: naming.boolean-prefix requires the prefix at the start of the identifier") - `src/Rules/Naming/BooleanPrefixRule.php` (search: `allowedPrefixes`) checks that bool-returning methods, bool parameters, and bool properties *begin* with one of the configured prefixes (default…
- [Vendored code under `src/Vendor/` evades the vendor filter](rules.md) (search: "## Footgun: Vendored code under `src/Vendor/` evades the vendor filter") - `design.single-implementor-interface` (and any future project rule) excludes files whose displayPath starts with `vendor/` by default, matching the Composer convention.
- [Retiring a rule leaves stale count references in five doc artefacts](rules.md) (search: "## Footgun: Retiring a rule leaves stale count references in five doc artefacts") - The rule registry's true count lives only in `src/Rules/RuleRegistry.php` (the `NAMING_RULE_PRIORITY` constant plus the public registration block), but human-readable counts of the same facts are…
- [PHPStan/Psalm array-shape exemptions need a "concrete sibling" gate, not "any nested mixed"](rules.md) (search: "## Footgun: PHPStan/Psalm array-shape exemptions need a") - `src/Rules/Modernisation/PhpDocMixedOveruseRule.php` (search: `isPreciseArrayShape`) exempts `array{...}` shapes that name at least one sibling field with a non-mixed type, on the basis that the…
- [PHPStan rejects prose attached to multiline array-shape tags](rules.md) (search: "## Footgun: PHPStan rejects prose attached to multiline array-shape tags") - `docs.return-comment` only needs to know whether an `@return` tag has prose after the type, and `src/Rules/Docs/PhpdocTagText.php` (search: `returnTagBody`) now reads multiline array-shape tags…
- [Member-existence rules must honour PHP's split case rules — methods case-insensitive, properties case-sensitive](rules.md) (search: "## Footgun: Member-existence rules must honour PHP's split case rules — methods case-insensitive, properties case-sensitive") - PHP resolves method names case-insensitively but property names case-sensitively: `method_exists($c, 'RENDER')` is true for a declared `render()`, yet `property_exists($c, 'LABEL')` is false for a…
- [`NodeIndex` enumerates declarations nested in functions and conditionals, not just top-level ones](rules.md) (search: "## Footgun: `NodeIndex` enumerates declarations nested in functions and conditionals, not just top-level ones") - `NodeIndex::nodesOfAny`/`nodesOf` return matches from a full preorder walk of the whole unit — `src/Rules/Shared/NodeIndex.php` (search: `traverse($analysisUnit->statements)`) visits every…
- [Same-class callable-array detection must include `[__CLASS__, 'method']`, not just `$this`/`self::class`/`static::class`/`Class::class`](rules.md) (search: "## Footgun: Same-class callable-array detection must include `[__CLASS__, 'method']`, not just `$this`/`self::class`/`static::class`/`Class::class`") - `src/Rules/DeadCode/UnusedPrivateMethodRule.php` (search: `isCallableReference`) treats a private method as used when it appears in a callable array `[$this, 'm']`, `[self::class, 'm']`,…
- [php-parser node-list properties type as `array<Stmt>` (int|string keys), so PHPStan L10 rejects an `array<int, Stmt>` parameter](rules.md) (search: "## Footgun: php-parser node-list properties type as `array<Stmt>` (int|string keys), so PHPStan L10 rejects an `array<int, Stmt>` parameter") - php-parser declares list properties such as `If_::$stmts`, `ClassMethod::$stmts`, and `Class_::$stmts` as `Stmt[]`, which PHPStan reads as `array<Stmt>` (key type `int|string`), not `array<int,…
- ["Data carrier / value object" detection must classify behaviour by RETURN type, not by parameter presence](rules.md) (search: "## Footgun:") - The size rules treat immutable value objects gently: `src/Rules/Size/PropertyCountRule.php` (search: `isReadonlyDataCarrier`) softens a high property count to advisory for a final readonly data…
- [Shared serializers fan out into multiple versioned schemas](schemas.md) (search: "## Footgun: Shared serializers fan out into multiple versioned schemas") - `src/Results/Scoring/FileScore.php` (search: `public function toArray`) and `src/Results/Scoring/PillarScore.php` (search: `public function toArray`) are embedded by two versioned payloads:…
- [Schema-version strings are stamped in many human-written places](schemas.md) (search: "## Footgun: Schema-version strings are stamped in many human-written places") - A `SCHEMA_VERSION` constant in PHP is just one of N stamps of the version string.
- [The `gruff-php.config.v0.1` literal lives in two source-of-truth places plus user-facing surfaces](schemas.md) (search: "## Footgun: The `gruff-php.config.v0.1` literal lives in two source-of-truth places plus user-facing surfaces") - ADR-015 introduced `schemaVersion: gruff-php.config.v0.1` as a required top-level key in `.gruff-php.yaml`.
- [Package bin bootstraps must use Composer's consumer autoloader](setup.md) (search: "## Footgun: Package bin bootstraps must use Composer's consumer autoloader") - `bin/gruff-php` (search: `$GLOBALS['_composer_autoload_path']`) must prefer Composer's generated bin-proxy autoload path when run from an installed project's `vendor/bin/gruff-php`.
- [Consumer install tests can resolve newer dependency majors than the source checkout](setup.md) (search: "## Footgun: Consumer install tests can resolve newer dependency majors than the source checkout") - `composer.json` (search: `"symfony/console": "^6.4 || ^7.0 || ^8.0"`) allows Symfony Console 8 on PHP 8.4, while the source checkout lockfile on PHP 8.3 currently installs Symfony Console 7.
- [Anonymous-class test fakes are scored by gruff's production rules](tests.md) (search: "## Footgun: Anonymous-class test fakes are scored by gruff's production rules") - The default `php bin/gruff-php analyse` scan invoked by `composer check` and `scripts/preflight-checks.sh` (search: `gruff_php_check`) does not exclude `tests/`.
- [The obvious data-provider consolidation trips phpdoc-mixed-overuse and the public-method cap](tests.md) (search: "## Footgun: The obvious data-provider consolidation trips phpdoc-mixed-overuse and the public-method cap") - `test-quality.repeated-structure-missing-data-provider` (`src/Rules/TestQuality/RepeatedStructureMissingDataProviderRule.php`, search: `MIN_GROUP_SIZE`) pushes three-plus structurally-identical tests…
- [Two split-hex snapshot hashes lock the rule corpus and rule definitions; both break silently on fixture and defaultOptions changes](tests.md) (search: "## Footgun: Two split-hex snapshot hashes lock the rule corpus and rule definitions; both break silently on fixture and defaultOptions changes") - Two regression tests pin a SHA-256 over a serialized snapshot, and each is sensitive to a change a different rule edit makes:
