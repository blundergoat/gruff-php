{"stats":{"totalMutantsCount":210,"killedCount":128,"killedByStaticAnalysisCount":0,"notCoveredCount":0,"escapedCount":82,"errorCount":0,"syntaxErrorCount":0,"skippedCount":0,"ignoredCount":0,"timeOutCount":0,"msi":60.95,"mutationCodeCoverage":100,"coveredCodeMsi":60.95},"escaped":[{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) (count($this->mutants) ?? $this->stats['totalMutantsCount']);\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":22},"diff":"@@ @@\n\n     public function totalMutants(): int\n     {\n-        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n+        return (int) (count($this->mutants) ?? $this->stats['totalMutantsCount']);\n     }\n\n     public function msi(): float","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.13e49a617f939ddd4b37f6c5c864fd74.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"OneZeroFloat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 1.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":27},"diff":"@@ @@\n\n     public function msi(): float\n     {\n-        return (float) ($this->stats['msi'] ?? 0.0);\n+        return (float) ($this->stats['msi'] ?? 1.0);\n     }\n\n     public function coveredMsi(): float","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.91df32ba1be98efa856c2f14554e8e80.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.014, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"OneZeroFloat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 1.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":32},"diff":"@@ @@\n\n     public function coveredMsi(): float\n     {\n-        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n+        return (float) ($this->stats['coveredCodeMsi'] ?? 1.0);\n     }\n\n     public function coverageRate(): float","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.524fbf3863dc2b4bce703d009267de59.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"OneZeroFloat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 1.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":37},"diff":"@@ @@\n\n     public function coverageRate(): float\n     {\n-        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n+        return (float) ($this->stats['mutationCodeCoverage'] ?? 1.0);\n     }\n\n     \/**","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.9e798599ad6981457b3b98cb6b0820a5.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapArrayValues","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        );\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":45},"diff":"@@ @@\n      *\/\n     public function survivedMutants(): array\n     {\n-        return array_values(array_filter(\n+        return array_filter(\n             $this->mutants,\n             static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n-        ));\n+        );\n     }\n\n     \/**","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d63257c24d70ce9468ad23adcaf683a4.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IfNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (!in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":63},"diff":"@@ @@\n             $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n-            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n+            if (!in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                 $byFile[$mutant->filePath]['killed']++;\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d68ddc624278669c2605a6df8dae29ed.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IfNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (!in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":67},"diff":"@@ @@\n                 $byFile[$mutant->filePath]['killed']++;\n             }\n\n-            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n+            if (!in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                 $byFile[$mutant->filePath]['survived']++;\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.8c8d74ad65a06b3ccdd6e58353155f9a.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"FunctionCallRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        \n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":76},"diff":"@@ @@\n             }\n         }\n\n-        ksort($byFile, SORT_STRING);\n+\n         $summaries = [];\n\n         foreach ($byFile as $file => $counts) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.afecee7f2b673964c50c2c0e3e61cedd.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Plus","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested - $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":82},"diff":"@@ @@\n         foreach ($byFile as $file => $counts) {\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n-            $msiDenominator = $tested + $counts['notCovered'];\n+            $msiDenominator = $tested - $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6e50e041d27f753bce8eb3dcda8fd26c.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === -1 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === -1 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2d706f35ff029f38c76c4d0d99bdd39e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"OneZeroFloat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 1.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 1.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c903eeed97e18c2feb3da98cd6d5b9f5.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 1);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 1);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.cafc78910908a5d7196b9baeef813319.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 3);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 3);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.da46798f6dc423d64356936e9de4645e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"RoundingFamily","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : floor(($counts['killed'] \/ $msiDenominator) * 100);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : floor(($counts['killed'] \/ $msiDenominator) * 100);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.15e02bc3362228347df00a01b1230fb1.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"RoundingFamily","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : ceil(($counts['killed'] \/ $msiDenominator) * 100);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : ceil(($counts['killed'] \/ $msiDenominator) * 100);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.8342bfca7c40460fafe46249640cda2b.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === -1 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === -1 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.02088f9e10c83fe9b861dfc6cae92827.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"OneZeroFloat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 1.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 1.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.706da88f69debe6660aae868c6e45a4e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Division","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] * $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] * $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ec73fd51f2ba6342850da24218f892d1.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 99, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 99, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.e1e3d42c3600a6bca2ea9ff2c4614ac4.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 101, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 101, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6bf075135b305242242af5c920d43a22.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Multiplication","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round($counts['killed'] \/ $covered \/ 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round($counts['killed'] \/ $covered \/ 100, 2);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d4afa4114d234fd6b63b6162d6ae8d95.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 1);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 1);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.256cefec2a133c5d633d9480f705c00c.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 3);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 3);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.b31f30582e0c04cebbb126a006cf6811.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"RoundingFamily","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : floor(($counts['killed'] \/ $covered) * 100);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : floor(($counts['killed'] \/ $covered) * 100);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.69a303fc85023d298b7136164b7f77c5.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"RoundingFamily","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : ceil(($counts['killed'] \/ $covered) * 100);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? 0.0 : ceil(($counts['killed'] \/ $covered) * 100);\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c98d80879999298876c8e24afa279d19.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? round(($counts['killed'] \/ $covered) * 100, 2) : 0.0;\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":84},"diff":"@@ @@\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n-            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n+            $coveredMsi = $covered === 0 ? round(($counts['killed'] \/ $covered) * 100, 2) : 0.0;\n\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a889500a11f79db3308b8a3e098e41ac.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ArrayOneItem","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return count($summaries) > 1 ? array_slice($summaries, 0, 1, true) : $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":97},"diff":"@@ @@\n             );\n         }\n\n-        return $summaries;\n+        return count($summaries) > 1 ? array_slice($summaries, 0, 1, true) : $summaries;\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.e88a30a493af654f87ce1e19ec3327de.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 511, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":25},"diff":"@@ @@\n         }\n\n         try {\n-            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n+            $decoded = json_decode($contents, true, 511, JSON_THROW_ON_ERROR);\n         } catch (JsonException $exception) {\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.451b9757a6eea837da87c6410686ef73.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 513, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":25},"diff":"@@ @@\n         }\n\n         try {\n-            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n+            $decoded = json_decode($contents, true, 513, JSON_THROW_ON_ERROR);\n         } catch (JsonException $exception) {\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.985b9a2e0fb671371950f544d8e604ff.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) && array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":30},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }\n\n-        if (!is_array($decoded) || array_is_list($decoded)) {\n+        if (!is_array($decoded) && array_is_list($decoded)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d524276b90cc2026772c1d204f182f8d.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) && !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":39},"diff":"@@ @@\n\n         foreach ($this->statusSections() as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n-            if (!is_array($rows) || !array_is_list($rows)) {\n+            if (!is_array($rows) && !array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.26690d4cee45e76ff16edc29df47e4cc.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[1] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[1] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2411e1b3808f6175cd6c2669dd58c932.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $this->projectRoot . '\/' . $path : $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $this->projectRoot . '\/' . $path : $path;\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6f6f1c6ebee311d2d4f82814a8766be2.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) && array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":71},"diff":"@@ @@\n     private function parseStats(array $decoded, string $path): array\n     {\n         $rawStats = $decoded['stats'] ?? null;\n-        if (!is_array($rawStats) || array_is_list($rawStats)) {\n+        if (!is_array($rawStats) && array_is_list($rawStats)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ef96635000f444676d1e7713c05963d8.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ArrayItemRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":84},"diff":"@@ @@\n             $stats[$key] = $value;\n         }\n\n-        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n+        foreach (['msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n             if (!array_key_exists($requiredKey, $stats)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0e8aca0e68c975f8b3ad1305ac45b4e9.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach ([] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":84},"diff":"@@ @@\n             $stats[$key] = $value;\n         }\n\n-        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n+        foreach ([] as $requiredKey) {\n             if (!array_key_exists($requiredKey, $stats)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.b975fd100570bef3b89b74902f6e3cd4.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) && array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":95},"diff":"@@ @@\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n     {\n-        if (!is_array($row) || array_is_list($row)) {\n+        if (!is_array($row) && array_is_list($row)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.b58231a7f06e2d5b4539b2c0164b7434.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) && array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":100},"diff":"@@ @@\n         }\n\n         $mutator = $row['mutator'] ?? null;\n-        if (!is_array($mutator) || array_is_list($mutator)) {\n+        if (!is_array($mutator) && array_is_list($mutator)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c1b79fff87c778fa8badc668f2d15bcf.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) && $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":108},"diff":"@@ @@\n         $filePath = $mutator['originalFilePath'] ?? null;\n         $line = $mutator['originalStartLine'] ?? null;\n\n-        if (!is_string($mutatorName) || $mutatorName === '') {\n+        if (!is_string($mutatorName) && $mutatorName === '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0be6690f38e0dcf5d2256135535a001c.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) && $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":112},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }\n\n-        if (!is_string($filePath) || $filePath === '') {\n+        if (!is_string($filePath) && $filePath === '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.430d61ddd185b3c814ec50070a75fb22.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.014, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line === null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":116},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }\n\n-        if ($line !== null && !is_int($line)) {\n+        if ($line === null && !is_int($line)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.37d707c649146b7e384bb70f90da7ecd.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if (!($line !== null) && is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":116},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }\n\n-        if ($line !== null && !is_int($line)) {\n+        if (!($line !== null) && is_int($line)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.9970e49c5614585ee88fa777e14fcb0b.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff === '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: is_string($diff) && $diff === '' ? $diff : null,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0a87a9f6362066e1a60dee45e3a7cb02.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) || $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: is_string($diff) || $diff !== '' ? $diff : null,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.85a88e4561cd8221602e2201935763b2.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: !is_string($diff) && !($diff !== '') ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: !is_string($diff) && !($diff !== '') ? $diff : null,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3f62ab4275741c86d21ae6ed32246723.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.012, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: !(is_string($diff) && $diff !== '') ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: !(is_string($diff) && $diff !== '') ? $diff : null,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d83c48235c9008d1553f73c14127e501.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: !is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: !is_string($diff) && $diff !== '' ? $diff : null,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.49871ae6c15a13764a0d750ab73577bf.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.012, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? null : $diff,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":128},"diff":"@@ @@\n             filePath: $this->displayPath($filePath),\n             line: $line,\n             mutator: $mutatorName,\n-            diff: is_string($diff) && $diff !== '' ? $diff : null,\n+            diff: is_string($diff) && $diff !== '' ? null : $diff,\n             processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.736ef8b867f0541c3e496dc2fd6dacb8.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput === '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: is_string($processOutput) && $processOutput === '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.85288935b8c7c80f5d3cf542e1be2dbd.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) || $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: is_string($processOutput) || $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.919fd2f14f22e25cbc2f3ad57438e74e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: !is_string($processOutput) && !($processOutput !== '') ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: !is_string($processOutput) && !($processOutput !== '') ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.10bfd415d4ee2d21cd4aaacda71db798.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: !(is_string($processOutput) && $processOutput !== '') ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: !(is_string($processOutput) && $processOutput !== '') ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d6aeb4715570b0fa3ed0c4e924f204b9.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: !is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: !is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.da4f85efca278ef84617cf880a01971d.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? null : $processOutput,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":129},"diff":"@@ @@\n             line: $line,\n             mutator: $mutatorName,\n             diff: is_string($diff) && $diff !== '' ? $diff : null,\n-            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n+            processOutput: is_string($processOutput) && $processOutput !== '' ? null : $processOutput,\n         );\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2db04d40839a2e26cdc057a12253ecf6.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapStrReplace","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = $path;\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":152},"diff":"@@ @@\n\n     private function displayPath(string $path): string\n     {\n-        $normalizedPath = str_replace('\\\\', '\/', $path);\n+        $normalizedPath = $path;\n         $realProjectRoot = realpath($this->projectRoot);\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3e6e6573bc918e449166ba24e2f5555b.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapStrReplace","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot, '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":154},"diff":"@@ @@\n     {\n         $normalizedPath = str_replace('\\\\', '\/', $path);\n         $realProjectRoot = realpath($this->projectRoot);\n-        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n+        $normalizedRoot = rtrim(is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot, '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3b49b8f68d6a647d9e0cb69d6c09273d.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapRtrim","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot);\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":154},"diff":"@@ @@\n     {\n         $normalizedPath = str_replace('\\\\', '\/', $path);\n         $realProjectRoot = realpath($this->projectRoot);\n-        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n+        $normalizedRoot = str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot);\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.04ed8c749c07e957d33756ae4a249203.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.012, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":156},"diff":"@@ @@\n         $realProjectRoot = realpath($this->projectRoot);\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n-        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n+        if (str_starts_with($normalizedPath, '\/')) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2dc1b882550e7f5d5d0338280b34e9f6.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot)) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":156},"diff":"@@ @@\n         $realProjectRoot = realpath($this->projectRoot);\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n-        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n+        if (str_starts_with($normalizedPath, $normalizedRoot)) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.4c27ba590f6d910c04694e297577be30.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $this->defaultConfigPath($projectRoot) ?? $configPath;\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":35},"diff":"@@ @@\n             );\n         }\n\n-        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n+        $effectiveConfigPath = $this->defaultConfigPath($projectRoot) ?? $configPath;\n         $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n         if ($effectiveConfigPath !== null) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.794fe58357323ea1856cebef1942eafb.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapTrim","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && $testFrameworkOptions !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if ($testFrameworkOptions !== null && $testFrameworkOptions !== '') {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.52a6c0a31f29feabebbd37c7ada3e1a6.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"MethodCallRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        \n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":48},"diff":"@@ @@\n         }\n\n         $process = new Process($command, $projectRoot);\n-        $process->setTimeout(null);\n+\n         $process->run();\n\n         return new InfectionRunResult(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0b982e413b90942a499f5e16d7221724.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 1,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":52},"diff":"@@ @@\n         $process->run();\n\n         return new InfectionRunResult(\n-            exitCode: $process->getExitCode() ?? 2,\n+            exitCode: $process->getExitCode() ?? 1,\n             output: $process->getOutput(),\n             errorOutput: $process->getErrorOutput(),\n         );","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.90646d148ad01a7eb00601bd08462a94.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 3,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":52},"diff":"@@ @@\n         $process->run();\n\n         return new InfectionRunResult(\n-            exitCode: $process->getExitCode() ?? 2,\n+            exitCode: $process->getExitCode() ?? 3,\n             output: $process->getOutput(),\n             errorOutput: $process->getErrorOutput(),\n         );","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f81d62cf1c3bd4ddde097db3ca05d750.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') && str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":64},"diff":"@@ @@\n             return null;\n         }\n\n-        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n+        if (str_contains($binary, '\/') && str_contains($binary, '\\\\')) {\n             $candidate = $this->absolutePath($projectRoot, $binary);\n\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.e6db055a6429bb1e37b1752669677637.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapRtrim","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = $projectRoot . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = $projectRoot . '\/vendor\/bin\/' . $binary;\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f0509593521b9ca4c647bbf5f51a1e9c.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = '\/vendor\/bin\/' . rtrim($projectRoot, '\/') . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = '\/vendor\/bin\/' . rtrim($projectRoot, '\/') . $binary;\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dda44b30d71f0fdab37dcf4958394be5.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = '\/vendor\/bin\/' . $binary;\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5f2dd4d469d00e288fd6cf61565e63a2.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = rtrim($projectRoot, '\/') . $binary;\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f1606e1fb8b2e0c889a3966f5c1f92a1.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . $binary . '\/vendor\/bin\/';\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = rtrim($projectRoot, '\/') . $binary . '\/vendor\/bin\/';\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.505f52c1daaf302dcfd0efe69767ec81.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/';\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":70},"diff":"@@ @@\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n         }\n\n-        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n+        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/';\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a96ebe14f98b41ca1556ab12d743cb13.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) || is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":72},"diff":"@@ @@\n\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n-        if (is_file($localBinary) && is_executable($localBinary)) {\n+        if (is_file($localBinary) || is_executable($localBinary)) {\n             return $localBinary;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.461e75ef8ad2085f37c45ce6ef3f32d2.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (!is_file($localBinary) && !is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":72},"diff":"@@ @@\n\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n-        if (is_file($localBinary) && is_executable($localBinary)) {\n+        if (!is_file($localBinary) && !is_executable($localBinary)) {\n             return $localBinary;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a21544a8d9cfc91e4954b590db43f6b2.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (!(is_file($localBinary) && is_executable($localBinary))) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":72},"diff":"@@ @@\n\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n-        if (is_file($localBinary) && is_executable($localBinary)) {\n+        if (!(is_file($localBinary) && is_executable($localBinary))) {\n             return $localBinary;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5fc2f2e91b9db762277890551826537e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (!is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":72},"diff":"@@ @@\n\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n-        if (is_file($localBinary) && is_executable($localBinary)) {\n+        if (!is_file($localBinary) && is_executable($localBinary)) {\n             return $localBinary;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.24089160ab48a1747be7aab04aefcbf0.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && !is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":72},"diff":"@@ @@\n\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n-        if (is_file($localBinary) && is_executable($localBinary)) {\n+        if (is_file($localBinary) && !is_executable($localBinary)) {\n             return $localBinary;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.1856ed112964d0d7de09061c91d2d956.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"ReturnRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            \n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":73},"diff":"@@ @@\n         $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n         if (is_file($localBinary) && is_executable($localBinary)) {\n-            return $localBinary;\n+\n         }\n\n         $resolved = (new ExecutableFinder())->find($binary);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5525aaa7c496aafbb19673774702dee3.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapRtrim","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file($projectRoot . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":83},"diff":"@@ @@\n\n     private function defaultConfigPath(string $projectRoot): ?string\n     {\n-        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n+        return is_file($projectRoot . '\/infection.json5') ? 'infection.json5' : null;\n     }\n\n     private function absolutePath(string $projectRoot, string $path): string","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c1b6ab93e6d64bc544b23442ede7610e.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path === '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if ($path === '' && $path[0] === '\/') {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.429e0298c5b6d4b9d747316153c3c2ca.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.008, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[1] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if ($path !== '' && $path[1] === '\/') {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3a5f89b1ce0fc83038d49858912d592c.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if (!($path !== '') && !($path[0] === '\/')) {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if (!($path !== '') && !($path[0] === '\/')) {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d2527928b15fc4a4aa6f4766857c4232.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"},{"mutator":{"mutatorName":"UnwrapRtrim","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return $projectRoot . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return $projectRoot . '\/' . $path;\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dae2716a9c0d45311828d22e75d97234.infection.xml\n\n.....                                                               5 \/ 5 (100%)\n\nTime: 00:00.009, Memory: 10.00 MB\n\nOK (5 tests, 32 assertions)"}],"timeouted":[],"killed":[{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    protected function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":20},"diff":"@@ @@\n     ) {\n     }\n\n-    public function totalMutants(): int\n+    protected function totalMutants(): int\n     {\n         return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ce0dd2fb60222f10b33ef00a68db6f4d.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::totalMutants() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:26\n\nERRORS!\nTests: 1, Assertions: 1, Errors: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    protected function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":25},"diff":"@@ @@\n         return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n     }\n\n-    public function msi(): float\n+    protected function msi(): float\n     {\n         return (float) ($this->stats['msi'] ?? 0.0);\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f9a3b349cf0dee2e14864733a132a9e0.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::msi() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:27\n\nERRORS!\nTests: 1, Assertions: 2, Errors: 1."},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) (0.0 ?? $this->stats['msi']);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":27},"diff":"@@ @@\n\n     public function msi(): float\n     {\n-        return (float) ($this->stats['msi'] ?? 0.0);\n+        return (float) (0.0 ?? $this->stats['msi']);\n     }\n\n     public function coveredMsi(): float","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2c8b34a35729d61933f9ad849a8e8255.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:27\n\nFAILURES!\nTests: 1, Assertions: 3, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    protected function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":30},"diff":"@@ @@\n         return (float) ($this->stats['msi'] ?? 0.0);\n     }\n\n-    public function coveredMsi(): float\n+    protected function coveredMsi(): float\n     {\n         return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.90ac9f7bacd40b0154fa7f46e3262209.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::coveredMsi() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:28\n\nERRORS!\nTests: 1, Assertions: 3, Errors: 1."},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) (0.0 ?? $this->stats['coveredCodeMsi']);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":32},"diff":"@@ @@\n\n     public function coveredMsi(): float\n     {\n-        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n+        return (float) (0.0 ?? $this->stats['coveredCodeMsi']);\n     }\n\n     public function coverageRate(): float","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a928b6c067406e5b9c27fe0cee686f20.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:28\n\nFAILURES!\nTests: 1, Assertions: 4, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    protected function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":35},"diff":"@@ @@\n         return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n     }\n\n-    public function coverageRate(): float\n+    protected function coverageRate(): float\n     {\n         return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n     }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.49e9e42c5f67e349054781640b55f09d.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::coverageRate() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:29\n\nERRORS!\nTests: 1, Assertions: 4, Errors: 1."},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) (0.0 ?? $this->stats['mutationCodeCoverage']);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":37},"diff":"@@ @@\n\n     public function coverageRate(): float\n     {\n-        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n+        return (float) (0.0 ?? $this->stats['mutationCodeCoverage']);\n     }\n\n     \/**","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.1124e045fb6b1d73ff723a02751661c2.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 100.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:29\n\nFAILURES!\nTests: 1, Assertions: 5, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    protected function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":43},"diff":"@@ @@\n     \/**\n      * @return list<InfectionMutant>\n      *\/\n-    public function survivedMutants(): array\n+    protected function survivedMutants(): array\n     {\n         return array_values(array_filter(\n             $this->mutants,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.85307d7458f655111d7265efdeb0e62e.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::survivedMutants() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nERRORS!\nTests: 1, Assertions: 5, Errors: 1."},{"mutator":{"mutatorName":"UnwrapArrayFilter","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values($this->mutants);\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":45},"diff":"@@ @@\n      *\/\n     public function survivedMutants(): array\n     {\n-        return array_values(array_filter(\n-            $this->mutants,\n-            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n-        ));\n+        return array_values($this->mutants);\n     }\n\n     \/**","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3421c313f7a5e4bae409f1c3fb307818.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 4 matches expected size 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nFAILURES!\nTests: 1, Assertions: 6, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    protected function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":54},"diff":"@@ @@\n     \/**\n      * @return list<MutationFileSummary>\n      *\/\n-    public function fileSummaries(): array\n+    protected function fileSummaries(): array\n     {\n         \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n         $byFile = [];","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.20f44e33daa3f6700310a8f60899b635.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReport::fileSummaries() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:32\n\nERRORS!\nTests: 1, Assertions: 6, Errors: 1."},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ([] as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":59},"diff":"@@ @@\n         \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n         $byFile = [];\n\n-        foreach ($this->mutants as $mutant) {\n+        foreach ([] as $mutant) {\n             $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n-\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                 $byFile[$mutant->filePath]['killed']++;\n             }\n-\n             if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                 $byFile[$mutant->filePath]['survived']++;\n             }\n-\n             if ($mutant->status === 'not covered') {\n                 $byFile[$mutant->filePath]['notCovered']++;\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c316fb5dd4907f3e835ae5b1c612ea3b.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 0 matches expected size 1.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:34\n\nFAILURES!\nTests: 1, Assertions: 7, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 1, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 1, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f293278cc01fdcab5ec9cf32a9ca8d45.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 5 is identical to 4.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:36\n\nFAILURES!\nTests: 1, Assertions: 9, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => -1, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => -1, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5268ada1e1fb6373eaca7cb34b653a6e.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 3 is identical to 4.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:36\n\nFAILURES!\nTests: 1, Assertions: 9, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => -1, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => -1, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d6076318ddfa707817ffcc346bde9c34.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 33.33 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 1, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 1, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.eb608ab1599982b3beedf563211fbdc0.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 60.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => -1, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => -1, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.607ba67cf571491e9c687ec0a4a6afd2.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 1 is identical to 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:37\n\nFAILURES!\nTests: 1, Assertions: 10, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 1, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 1, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.7acc27767baea21325ab115607fe275d.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 3 is identical to 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:37\n\nFAILURES!\nTests: 1, Assertions: 10, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => -1];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => -1];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2a2b7bdfccdfd171bfbb037a73d32ddb.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 66.67 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 1];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 1];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0dc641cbd366b6012e22454c3e7d1cf7.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 40.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"ArrayItemRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] ??= ['killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2cf65e2308fd12f52b59848edd0e8705.infection.xml\n\n....W                                                               5 \/ 5 (100%)\n\nTime: 00:00.010, Memory: 10.00 MB\n\n1 test triggered 1 PHP warning:\n\n1) \/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php:61\nUndefined array key \"total\"\n\nTriggered by:\n\n* GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\n  \/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:19\n\nOK, but there were issues!\nTests: 5, Assertions: 32, Warnings: 1."},{"mutator":{"mutatorName":"AssignCoalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] = ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":60},"diff":"@@ @@\n         $byFile = [];\n\n         foreach ($this->mutants as $mutant) {\n-            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n+            $byFile[$mutant->filePath] = ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.90f3cfc53ad931d1df3f7b4495ee73c3.infection.xml\n\nF\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 1 is identical to 4.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:36\n\nFAILURES!\nTests: 1, Assertions: 9, Failures: 1."},{"mutator":{"mutatorName":"Increment","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']--;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":61},"diff":"@@ @@\n\n         foreach ($this->mutants as $mutant) {\n             $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n-            $byFile[$mutant->filePath]['total']++;\n+            $byFile[$mutant->filePath]['total']--;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                 $byFile[$mutant->filePath]['killed']++;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.aa32c011feb2e75371e51494fccbcc32.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that -4 is identical to 4.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:36\n\nFAILURES!\nTests: 1, Assertions: 9, Failures: 1."},{"mutator":{"mutatorName":"Increment","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']--;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":64},"diff":"@@ @@\n             $byFile[$mutant->filePath]['total']++;\n\n             if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n-                $byFile[$mutant->filePath]['killed']++;\n+                $byFile[$mutant->filePath]['killed']--;\n             }\n\n             if (in_array($mutant->status, ['escaped', 'timed out'], true)) {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6c940117bf5276b9913998867e3addf7.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"Increment","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']--;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":68},"diff":"@@ @@\n             }\n\n             if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n-                $byFile[$mutant->filePath]['survived']++;\n+                $byFile[$mutant->filePath]['survived']--;\n             }\n\n             if ($mutant->status === 'not covered') {","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.bca51941e37103491fc860bb0cbebd01.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that -2 is identical to 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:37\n\nFAILURES!\nTests: 1, Assertions: 10, Failures: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status !== 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":71},"diff":"@@ @@\n                 $byFile[$mutant->filePath]['survived']++;\n             }\n\n-            if ($mutant->status === 'not covered') {\n+            if ($mutant->status !== 'not covered') {\n                 $byFile[$mutant->filePath]['notCovered']++;\n             }\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3e435526e34fbedac1335c43ba88ed53.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 25.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ([] as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":79},"diff":"@@ @@\n         ksort($byFile, SORT_STRING);\n         $summaries = [];\n\n-        foreach ($byFile as $file => $counts) {\n+        foreach ([] as $file => $counts) {\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n-\n             $summaries[] = new MutationFileSummary(\n                 filePath: $file,\n                 totalMutants: $counts['total'],","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a1399e6d0cdab8cd1920b15d227771f6.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 0 matches expected size 1.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:34\n\nFAILURES!\nTests: 1, Assertions: 7, Failures: 1."},{"mutator":{"mutatorName":"Plus","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] - $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":80},"diff":"@@ @@\n         $summaries = [];\n\n         foreach ($byFile as $file => $counts) {\n-            $tested = $counts['killed'] + $counts['survived'];\n+            $tested = $counts['killed'] - $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n             $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.e90136d900248810fdec9bb3edf168fc.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"Division","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] * $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] * $msiDenominator) * 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d8392698b836fa438556c7e391c990f2.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 800.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 99, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 99, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.cf239c6b67ebcd52d4e27b692ec59111.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 49.5 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 101, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 101, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.359782b8629fc442a5f9d5d2fa5a8f0e.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 50.5 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"Multiplication","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round($counts['killed'] \/ $msiDenominator \/ 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? 0.0 : round($counts['killed'] \/ $msiDenominator \/ 100, 2);\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0ded83f40d71fc66a3080aab809490ed.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.01 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nfinal readonly class InfectionReport\n{\n    \/**\n     * @param array<string, int|float> $stats\n     * @param list<InfectionMutant> $mutants\n     *\/\n    public function __construct(\n        public string $reportPath,\n        public array $stats,\n        public array $mutants,\n    ) {\n    }\n\n    public function totalMutants(): int\n    {\n        return (int) ($this->stats['totalMutantsCount'] ?? count($this->mutants));\n    }\n\n    public function msi(): float\n    {\n        return (float) ($this->stats['msi'] ?? 0.0);\n    }\n\n    public function coveredMsi(): float\n    {\n        return (float) ($this->stats['coveredCodeMsi'] ?? 0.0);\n    }\n\n    public function coverageRate(): float\n    {\n        return (float) ($this->stats['mutationCodeCoverage'] ?? 0.0);\n    }\n\n    \/**\n     * @return list<InfectionMutant>\n     *\/\n    public function survivedMutants(): array\n    {\n        return array_values(array_filter(\n            $this->mutants,\n            static fn (InfectionMutant $mutant): bool => in_array($mutant->status, ['escaped', 'timed out'], true),\n        ));\n    }\n\n    \/**\n     * @return list<MutationFileSummary>\n     *\/\n    public function fileSummaries(): array\n    {\n        \/** @var array<string, array{total: int, killed: int, survived: int, notCovered: int}> $byFile *\/\n        $byFile = [];\n\n        foreach ($this->mutants as $mutant) {\n            $byFile[$mutant->filePath] ??= ['total' => 0, 'killed' => 0, 'survived' => 0, 'notCovered' => 0];\n            $byFile[$mutant->filePath]['total']++;\n\n            if (in_array($mutant->status, ['killed', 'killed by SA'], true)) {\n                $byFile[$mutant->filePath]['killed']++;\n            }\n\n            if (in_array($mutant->status, ['escaped', 'timed out'], true)) {\n                $byFile[$mutant->filePath]['survived']++;\n            }\n\n            if ($mutant->status === 'not covered') {\n                $byFile[$mutant->filePath]['notCovered']++;\n            }\n        }\n\n        ksort($byFile, SORT_STRING);\n        $summaries = [];\n\n        foreach ($byFile as $file => $counts) {\n            $tested = $counts['killed'] + $counts['survived'];\n            $covered = $tested;\n            $msiDenominator = $tested + $counts['notCovered'];\n            $msi = $msiDenominator === 0 ? round(($counts['killed'] \/ $msiDenominator) * 100, 2) : 0.0;\n            $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n            $summaries[] = new MutationFileSummary(\n                filePath: $file,\n                totalMutants: $counts['total'],\n                killedMutants: $counts['killed'],\n                survivedMutants: $counts['survived'],\n                notCoveredMutants: $counts['notCovered'],\n                msi: $msi,\n                coveredMsi: $coveredMsi,\n            );\n        }\n\n        return $summaries;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReport.php","originalStartLine":83},"diff":"@@ @@\n             $tested = $counts['killed'] + $counts['survived'];\n             $covered = $tested;\n             $msiDenominator = $tested + $counts['notCovered'];\n-            $msi = $msiDenominator === 0 ? 0.0 : round(($counts['killed'] \/ $msiDenominator) * 100, 2);\n+            $msi = $msiDenominator === 0 ? round(($counts['killed'] \/ $msiDenominator) * 100, 2) : 0.0;\n             $coveredMsi = $covered === 0 ? 0.0 : round(($counts['killed'] \/ $covered) * 100, 2);\n\n             $summaries[] = new MutationFileSummary(","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.176ee21333d03829199ec6b3409eae23.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:38\n\nFAILURES!\nTests: 1, Assertions: 11, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    protected function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":15},"diff":"@@ @@\n     {\n     }\n\n-    public function parse(string $path): InfectionReport\n+    protected function parse(string $path): InfectionReport\n     {\n         $resolvedPath = $this->resolvePath($path);\n         $contents = file_get_contents($resolvedPath);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.98146ace2590634f3cc216cc8f5198ef.infection.xml\n\nE\n\nTime: 00:00.003, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nError: Call to protected method GruffPhp\\Mutation\\InfectionReportParser::parse() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents !== false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":20},"diff":"@@ @@\n         $resolvedPath = $this->resolvePath($path);\n         $contents = file_get_contents($resolvedPath);\n\n-        if ($contents === false) {\n+        if ($contents !== false) {\n             throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.4a102923faec55c2d6a603cd7d08527c.infection.xml\n\nE\n\nTime: 00:00.003, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Unable to read Infection report: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:21\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":30},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }\n\n-        if (!is_array($decoded) || array_is_list($decoded)) {\n+        if (is_array($decoded) || array_is_list($decoded)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a13e3c2fa5a0c6ca1fa260cab4ad9efa.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:31\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (is_array($decoded) || !array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":30},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }\n\n-        if (!is_array($decoded) || array_is_list($decoded)) {\n+        if (is_array($decoded) || !array_is_list($decoded)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.146711aae02ffc58ea1d05e786901d85.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:31\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!(!is_array($decoded) || array_is_list($decoded))) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":30},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }\n\n-        if (!is_array($decoded) || array_is_list($decoded)) {\n+        if (!(!is_array($decoded) || array_is_list($decoded))) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5478f4a1b1c86dd6b970534f29578c5e.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:31\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || !array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":30},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n         }\n\n-        if (!is_array($decoded) || array_is_list($decoded)) {\n+        if (!is_array($decoded) || !array_is_list($decoded)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.491101d6e6177154b3c5db8fa5e3fbdf.infection.xml\n\nE\n\nTime: 00:00.003, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:31\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ([] as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":37},"diff":"@@ @@\n         $stats = $this->parseStats($decoded, $path);\n         $mutants = [];\n\n-        foreach ($this->statusSections() as $section => $status) {\n+        foreach ([] as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n             if (!is_array($rows) || !array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }\n-\n             foreach ($rows as $index => $row) {\n                 $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.103d3a91945d514c23624fb8d5fc4930.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 0 matches expected size 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nFAILURES!\nTests: 1, Assertions: 6, Failures: 1."},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = [] ?? $decoded[$section];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":38},"diff":"@@ @@\n         $mutants = [];\n\n         foreach ($this->statusSections() as $section => $status) {\n-            $rows = $decoded[$section] ?? [];\n+            $rows = [] ?? $decoded[$section];\n             if (!is_array($rows) || !array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0df17a794984e4cf3a97d1c5145d3358.infection.xml\n\nF\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 0 matches expected size 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nFAILURES!\nTests: 1, Assertions: 6, Failures: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":39},"diff":"@@ @@\n\n         foreach ($this->statusSections() as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n-            if (!is_array($rows) || !array_is_list($rows)) {\n+            if (is_array($rows) || !array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f2b6bb2e08306c21f97286f30353ca45.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" section \"escaped\" must be a JSON array.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:40\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":39},"diff":"@@ @@\n\n         foreach ($this->statusSections() as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n-            if (!is_array($rows) || !array_is_list($rows)) {\n+            if (!is_array($rows) || array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.7d1d2ed06a72534654294c5d2dd93ba0.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" section \"escaped\" must be a JSON array.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:40\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (is_array($rows) || array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":39},"diff":"@@ @@\n\n         foreach ($this->statusSections() as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n-            if (!is_array($rows) || !array_is_list($rows)) {\n+            if (is_array($rows) || array_is_list($rows)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.84eb4211a7f665287c752decd9cbc768.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" section \"escaped\" must be a JSON array.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:40\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!(!is_array($rows) || !array_is_list($rows))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":39},"diff":"@@ @@\n\n         foreach ($this->statusSections() as $section => $status) {\n             $rows = $decoded[$section] ?? [];\n-            if (!is_array($rows) || !array_is_list($rows)) {\n+            if (!(!is_array($rows) || !array_is_list($rows))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5afd61f06698919e2683ce5af6fecf15.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" section \"escaped\" must be a JSON array.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:40\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ([] as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":43},"diff":"@@ @@\n                 throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n             }\n\n-            foreach ($rows as $index => $row) {\n+            foreach ([] as $index => $row) {\n                 $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n             }\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2e9f2989784d32dd13718455cbb81579.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 0 matches expected size 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nFAILURES!\nTests: 1, Assertions: 6, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : '\/' . $this->projectRoot . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $path : '\/' . $this->projectRoot . $path;\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a27e0889e740f95e948538238338d755.infection.xml\n\nE\n\nTime: 00:00.007, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $path : '\/' . $path;\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2c162dffd7b6747ec64024affd7886a4.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . $path;\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ace346b625a6defa05d7cb3890eeaedf.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . $path . '\/';\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . $path . '\/';\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5f350e58d5fd3a0297b07d2476ec1cce.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/';\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":53},"diff":"@@ @@\n\n     private function resolvePath(string $path): string\n     {\n-        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n+        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/';\n\n         if (!is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.862d780f478bbbd1846472b3bc6f0adb.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":55},"diff":"@@ @@\n     {\n         $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n-        if (!is_file($candidate)) {\n+        if (is_file($candidate)) {\n             throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.23a38c4473584043cd48bd67946e0732.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report not found: tests\/Fixtures\/M14\/Infection\/infection-valid.json\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:56\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:17\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $realPath : $candidate;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":61},"diff":"@@ @@\n\n         $realPath = realpath($candidate);\n\n-        return $realPath === false ? $candidate : $realPath;\n+        return $realPath === false ? $realPath : $candidate;\n     }\n\n     \/**","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2b5a72ee23b54aca8be18053e33fa92e.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'tests\/Mutation\/..\/..\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":71},"diff":"@@ @@\n     private function parseStats(array $decoded, string $path): array\n     {\n         $rawStats = $decoded['stats'] ?? null;\n-        if (!is_array($rawStats) || array_is_list($rawStats)) {\n+        if (is_array($rawStats) || array_is_list($rawStats)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.403f94f2c150e2f659a56999b33bf2ee.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must contain a \"stats\" object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:72\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (is_array($rawStats) || !array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":71},"diff":"@@ @@\n     private function parseStats(array $decoded, string $path): array\n     {\n         $rawStats = $decoded['stats'] ?? null;\n-        if (!is_array($rawStats) || array_is_list($rawStats)) {\n+        if (is_array($rawStats) || !array_is_list($rawStats)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.07ef65662300a300aa73b33454438b39.infection.xml\n\nE\n\nTime: 00:00.003, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must contain a \"stats\" object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:72\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!(!is_array($rawStats) || array_is_list($rawStats))) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":71},"diff":"@@ @@\n     private function parseStats(array $decoded, string $path): array\n     {\n         $rawStats = $decoded['stats'] ?? null;\n-        if (!is_array($rawStats) || array_is_list($rawStats)) {\n+        if (!(!is_array($rawStats) || array_is_list($rawStats))) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.7b1bc3b3701dfcea58d062cf4fda5a00.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must contain a \"stats\" object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:72\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || !array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":71},"diff":"@@ @@\n     private function parseStats(array $decoded, string $path): array\n     {\n         $rawStats = $decoded['stats'] ?? null;\n-        if (!is_array($rawStats) || array_is_list($rawStats)) {\n+        if (!is_array($rawStats) || !array_is_list($rawStats)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.788db84667e1611ce0bd8278bf53c774.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" must contain a \"stats\" object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:72\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Foreach_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ([] as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":76},"diff":"@@ @@\n         }\n\n         $stats = [];\n-        foreach ($rawStats as $key => $value) {\n+        foreach ([] as $key => $value) {\n             if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }\n-\n             $stats[$key] = $value;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.21df489159be9fbc53e4dee3eb4b32fa.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" is missing stats.totalMutantsCount.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:85\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (is_string($key) || (!is_int($value) && !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c1c509d605325dae56cd59cdcc2fb27e.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) || (!is_int($value) && is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.c323d3a8f6575c14e083e41f16ac14d9.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) || (is_int($value) && !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.977fa12b4b90f551083802b63495db45.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) || !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) || (!is_int($value) || !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.8c4d703565283c1166bfa02f5c35df09.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!(!is_int($value) && !is_float($value)))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) || (!(!is_int($value) && !is_float($value)))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.7d7b446040c315d59a6de95edf47acac.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (is_int($value) && is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) || (is_int($value) && is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.63be9983337c6247580c469fc700beda.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testRejectsMalformedInfectionJsonReport\nFailed asserting that exception message 'Infection report \"tests\/Fixtures\/M14\/Infection\/infection-malformed.json\" is missing stats.coveredCodeMsi.' contains 'contains a non-numeric stats value'.\n\nFAILURES!\nTests: 1, Assertions: 2, Failures: 1."},{"mutator":{"mutatorName":"LogicalOr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) && !is_int($value) && !is_float($value)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!is_string($key) && !is_int($value) && !is_float($value)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ed92f191af0582d87e1b1fe928cacd91.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testRejectsMalformedInfectionJsonReport\nFailed asserting that exception message 'Infection report \"tests\/Fixtures\/M14\/Infection\/infection-malformed.json\" is missing stats.coveredCodeMsi.' contains 'contains a non-numeric stats value'.\n\nFAILURES!\nTests: 1, Assertions: 2, Failures: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (is_string($key) || !(!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (is_string($key) || !(!is_int($value) && !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.1486d8555831fe1fb206955f847c22a0.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!(!is_string($key) || !is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":77},"diff":"@@ @@\n\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n-            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n+            if (!(!is_string($key) || !is_int($value) && !is_float($value))) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.8545c6aa891a4646cebf1fb5609937d8.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" contains a non-numeric stats value.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:78\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Throw_","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":78},"diff":"@@ @@\n         $stats = [];\n         foreach ($rawStats as $key => $value) {\n             if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n-                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n+                new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n             }\n\n             $stats[$key] = $value;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.083b309a7989de4d518e99280d630b7d.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testRejectsMalformedInfectionJsonReport\nFailed asserting that exception message 'Infection report \"tests\/Fixtures\/M14\/Infection\/infection-malformed.json\" is missing stats.coveredCodeMsi.' contains 'contains a non-numeric stats value'.\n\nFAILURES!\nTests: 1, Assertions: 2, Failures: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":85},"diff":"@@ @@\n         }\n\n         foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n-            if (!array_key_exists($requiredKey, $stats)) {\n+            if (array_key_exists($requiredKey, $stats)) {\n                 throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n             }\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dd14ee433179df53898dfec5335c7b64.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" is missing stats.totalMutantsCount.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:86\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:34\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"ArrayOneItem","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return count($stats) > 1 ? array_slice($stats, 0, 1, true) : $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":90},"diff":"@@ @@\n             }\n         }\n\n-        return $stats;\n+        return count($stats) > 1 ? array_slice($stats, 0, 1, true) : $stats;\n     }\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.755ba4760b4aef19330d438695be0996.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that 0.0 is identical to 50.0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:27\n\nFAILURES!\nTests: 1, Assertions: 3, Failures: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":95},"diff":"@@ @@\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n     {\n-        if (!is_array($row) || array_is_list($row)) {\n+        if (is_array($row) || array_is_list($row)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.87f597f8c139fbb6c512e78601978e86.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:96\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (is_array($row) || !array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":95},"diff":"@@ @@\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n     {\n-        if (!is_array($row) || array_is_list($row)) {\n+        if (is_array($row) || !array_is_list($row)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dd1d1aa5c23d3b5fa94821f115cbc654.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:96\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!(!is_array($row) || array_is_list($row))) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":95},"diff":"@@ @@\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n     {\n-        if (!is_array($row) || array_is_list($row)) {\n+        if (!(!is_array($row) || array_is_list($row))) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.883612bb3dcdb122f51b924252e5f749.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:96\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || !array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":95},"diff":"@@ @@\n\n     private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n     {\n-        if (!is_array($row) || array_is_list($row)) {\n+        if (!is_array($row) || !array_is_list($row)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.9ec0d05e91b1fbd42647b9ae0e06abc5.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must be a JSON object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:96\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":100},"diff":"@@ @@\n         }\n\n         $mutator = $row['mutator'] ?? null;\n-        if (!is_array($mutator) || array_is_list($mutator)) {\n+        if (is_array($mutator) || array_is_list($mutator)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.d73fdcb03ad6089484b6ac2accc0e2e0.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must contain a mutator object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:101\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (is_array($mutator) || !array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":100},"diff":"@@ @@\n         }\n\n         $mutator = $row['mutator'] ?? null;\n-        if (!is_array($mutator) || array_is_list($mutator)) {\n+        if (is_array($mutator) || !array_is_list($mutator)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ed001be615510147b9eff4b7e4d56b7a.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must contain a mutator object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:101\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!(!is_array($mutator) || array_is_list($mutator))) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":100},"diff":"@@ @@\n         }\n\n         $mutator = $row['mutator'] ?? null;\n-        if (!is_array($mutator) || array_is_list($mutator)) {\n+        if (!(!is_array($mutator) || array_is_list($mutator))) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.162d215c008a053b293aea0f592a71ae.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must contain a mutator object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:101\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || !array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":100},"diff":"@@ @@\n         }\n\n         $mutator = $row['mutator'] ?? null;\n-        if (!is_array($mutator) || array_is_list($mutator)) {\n+        if (!is_array($mutator) || !array_is_list($mutator)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.59f3fe64a0dc7a1706842eb6502d9f3b.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] must contain a mutator object.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:101\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":108},"diff":"@@ @@\n         $filePath = $mutator['originalFilePath'] ?? null;\n         $line = $mutator['originalStartLine'] ?? null;\n\n-        if (!is_string($mutatorName) || $mutatorName === '') {\n+        if (is_string($mutatorName) || $mutatorName === '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.bde65fb594b63e6c7e2d1dfb76f54ce1.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.mutatorName.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:109\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName !== '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":108},"diff":"@@ @@\n         $filePath = $mutator['originalFilePath'] ?? null;\n         $line = $mutator['originalStartLine'] ?? null;\n\n-        if (!is_string($mutatorName) || $mutatorName === '') {\n+        if (!is_string($mutatorName) || $mutatorName !== '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.af4b327fb576a0a7b3c70ddec9871f9e.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.mutatorName.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:109\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (is_string($mutatorName) || !($mutatorName === '')) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":108},"diff":"@@ @@\n         $filePath = $mutator['originalFilePath'] ?? null;\n         $line = $mutator['originalStartLine'] ?? null;\n\n-        if (!is_string($mutatorName) || $mutatorName === '') {\n+        if (is_string($mutatorName) || !($mutatorName === '')) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ad58a2821824750bb719c8bfd13826d7.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.mutatorName.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:109\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!(!is_string($mutatorName) || $mutatorName === '')) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":108},"diff":"@@ @@\n         $filePath = $mutator['originalFilePath'] ?? null;\n         $line = $mutator['originalStartLine'] ?? null;\n\n-        if (!is_string($mutatorName) || $mutatorName === '') {\n+        if (!(!is_string($mutatorName) || $mutatorName === '')) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ca2c96dc4c975b8a7cf762733d3e0c62.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.mutatorName.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:109\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":112},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }\n\n-        if (!is_string($filePath) || $filePath === '') {\n+        if (is_string($filePath) || $filePath === '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.1d5701226b004e432dd1484e577eedec.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.originalFilePath.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:113\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath !== '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":112},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }\n\n-        if (!is_string($filePath) || $filePath === '') {\n+        if (!is_string($filePath) || $filePath !== '') {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.68b5073e7d5def9b20e7b7ccd43540c5.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.originalFilePath.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:113\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (is_string($filePath) || !($filePath === '')) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":112},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }\n\n-        if (!is_string($filePath) || $filePath === '') {\n+        if (is_string($filePath) || !($filePath === '')) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.de0dd023f7b6d42f05fe253b1079a34a.infection.xml\n\nE\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.originalFilePath.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:113\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!(!is_string($filePath) || $filePath === '')) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":112},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n         }\n\n-        if (!is_string($filePath) || $filePath === '') {\n+        if (!(!is_string($filePath) || $filePath === '')) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ea07575a61796e744b50d4b039df3ace.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] is missing mutator.originalFilePath.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:113\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalNot","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":116},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }\n\n-        if ($line !== null && !is_int($line)) {\n+        if ($line !== null && is_int($line)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a47db1f7ee70e05466f007f9e7290034.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] has a non-integer mutator.originalStartLine.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:117\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null || !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":116},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }\n\n-        if ($line !== null && !is_int($line)) {\n+        if ($line !== null || !is_int($line)) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.753094d3c5ebcf1d3a3b4038bf7b2186.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] has a non-integer mutator.originalStartLine.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:117\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if (!($line !== null && !is_int($line))) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":116},"diff":"@@ @@\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n         }\n\n-        if ($line !== null && !is_int($line)) {\n+        if (!($line !== null && !is_int($line))) {\n             throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.39c30e5af9e70deedf78a08d7cb21758.infection.xml\n\nE\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nGruffPhp\\Mutation\\MutationReportException: Infection report \"tests\/Fixtures\/M14\/Infection\/infection-valid.json\" mutant escaped[0] has a non-integer mutator.originalStartLine.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:117\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php:44\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:21\n\nERRORS!\nTests: 1, Assertions: 0, Errors: 1."},{"mutator":{"mutatorName":"ArrayItemRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":138},"diff":"@@ @@\n     private function statusSections(): array\n     {\n         return [\n-            'escaped' => 'escaped',\n             'timeouted' => 'timed out',\n             'killed' => 'killed',\n             'killedByStaticAnalysis' => 'killed by SA',","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.31f4957d3e9e9003e4478687884db30f.infection.xml\n\nF\n\nTime: 00:00.007, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that actual size 1 matches expected size 2.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:30\n\nFAILURES!\nTests: 1, Assertions: 6, Failures: 1."},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $this->projectRoot : $realProjectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":154},"diff":"@@ @@\n     {\n         $normalizedPath = str_replace('\\\\', '\/', $path);\n         $realProjectRoot = realpath($this->projectRoot);\n-        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n+        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $this->projectRoot : $realProjectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.984327e5090abeae1545baef8a1d67a8.infection.xml\n\nF\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, '\/' . $normalizedRoot)) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":156},"diff":"@@ @@\n         $realProjectRoot = realpath($this->projectRoot);\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n-        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n+        if (str_starts_with($normalizedPath, '\/' . $normalizedRoot)) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.0c1feb57fc557b76eb79aca731990c60.infection.xml\n\nF\n\nTime: 00:00.007, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"IfNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (!str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":156},"diff":"@@ @@\n         $realProjectRoot = realpath($this->projectRoot);\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n-        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n+        if (!str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n             return substr($normalizedPath, strlen($normalizedRoot) + 1);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.b95ce91cc7301c6e6958996bf22bfe09.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"DecrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 0);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":157},"diff":"@@ @@\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n-            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n+            return substr($normalizedPath, strlen($normalizedRoot) + 0);\n         }\n\n         return ltrim($normalizedPath, '.\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.262f7c0c3c4424a700227542088ab015.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"IncrementInteger","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 2);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":157},"diff":"@@ @@\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n-            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n+            return substr($normalizedPath, strlen($normalizedRoot) + 2);\n         }\n\n         return ltrim($normalizedPath, '.\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.4adaa7ef1080426f36b67608bd2a04f3.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'ests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"Plus","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) - 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":157},"diff":"@@ @@\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n-            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n+            return substr($normalizedPath, strlen($normalizedRoot) - 1);\n         }\n\n         return ltrim($normalizedPath, '.\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.105fcf91d6a6320e299caf1fdce609f3.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'p\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"UnwrapSubstr","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return $normalizedPath;\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":157},"diff":"@@ @@\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n-            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n+            return $normalizedPath;\n         }\n\n         return ltrim($normalizedPath, '.\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2760826e3298e833e8e967d1d7949a1e.infection.xml\n\nF\n\nTime: 00:00.005, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"ReturnRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse JsonException;\n\nfinal readonly class InfectionReportParser\n{\n    public function __construct(private string $projectRoot)\n    {\n    }\n\n    public function parse(string $path): InfectionReport\n    {\n        $resolvedPath = $this->resolvePath($path);\n        $contents = file_get_contents($resolvedPath);\n\n        if ($contents === false) {\n            throw new MutationReportException(sprintf('Unable to read Infection report: %s', $path));\n        }\n\n        try {\n            $decoded = json_decode($contents, true, 512, JSON_THROW_ON_ERROR);\n        } catch (JsonException $exception) {\n            throw new MutationReportException(sprintf('Invalid Infection JSON report \"%s\": %s', $path, $exception->getMessage()), 0, $exception);\n        }\n\n        if (!is_array($decoded) || array_is_list($decoded)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must be a JSON object.', $path));\n        }\n\n        $stats = $this->parseStats($decoded, $path);\n        $mutants = [];\n\n        foreach ($this->statusSections() as $section => $status) {\n            $rows = $decoded[$section] ?? [];\n            if (!is_array($rows) || !array_is_list($rows)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" section \"%s\" must be a JSON array.', $path, $section));\n            }\n\n            foreach ($rows as $index => $row) {\n                $mutants[] = $this->parseMutant($row, $status, sprintf('%s[%d]', $section, $index), $path);\n            }\n        }\n\n        return new InfectionReport($this->displayPath($resolvedPath), $stats, $mutants);\n    }\n\n    private function resolvePath(string $path): string\n    {\n        $candidate = $path[0] === '\/' ? $path : $this->projectRoot . '\/' . $path;\n\n        if (!is_file($candidate)) {\n            throw new MutationReportException(sprintf('Infection report not found: %s', $path));\n        }\n\n        $realPath = realpath($candidate);\n\n        return $realPath === false ? $candidate : $realPath;\n    }\n\n    \/**\n     * @param array<mixed> $decoded\n     * @return array<string, int|float>\n     *\/\n    private function parseStats(array $decoded, string $path): array\n    {\n        $rawStats = $decoded['stats'] ?? null;\n        if (!is_array($rawStats) || array_is_list($rawStats)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" must contain a \"stats\" object.', $path));\n        }\n\n        $stats = [];\n        foreach ($rawStats as $key => $value) {\n            if (!is_string($key) || (!is_int($value) && !is_float($value))) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" contains a non-numeric stats value.', $path));\n            }\n\n            $stats[$key] = $value;\n        }\n\n        foreach (['totalMutantsCount', 'msi', 'coveredCodeMsi', 'mutationCodeCoverage'] as $requiredKey) {\n            if (!array_key_exists($requiredKey, $stats)) {\n                throw new MutationReportException(sprintf('Infection report \"%s\" is missing stats.%s.', $path, $requiredKey));\n            }\n        }\n\n        return $stats;\n    }\n\n    private function parseMutant(mixed $row, string $status, string $location, string $path): InfectionMutant\n    {\n        if (!is_array($row) || array_is_list($row)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must be a JSON object.', $path, $location));\n        }\n\n        $mutator = $row['mutator'] ?? null;\n        if (!is_array($mutator) || array_is_list($mutator)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s must contain a mutator object.', $path, $location));\n        }\n\n        $mutatorName = $mutator['mutatorName'] ?? null;\n        $filePath = $mutator['originalFilePath'] ?? null;\n        $line = $mutator['originalStartLine'] ?? null;\n\n        if (!is_string($mutatorName) || $mutatorName === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.mutatorName.', $path, $location));\n        }\n\n        if (!is_string($filePath) || $filePath === '') {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s is missing mutator.originalFilePath.', $path, $location));\n        }\n\n        if ($line !== null && !is_int($line)) {\n            throw new MutationReportException(sprintf('Infection report \"%s\" mutant %s has a non-integer mutator.originalStartLine.', $path, $location));\n        }\n\n        $diff = $row['diff'] ?? null;\n        $processOutput = $row['processOutput'] ?? null;\n\n        return new InfectionMutant(\n            status: $status,\n            filePath: $this->displayPath($filePath),\n            line: $line,\n            mutator: $mutatorName,\n            diff: is_string($diff) && $diff !== '' ? $diff : null,\n            processOutput: is_string($processOutput) && $processOutput !== '' ? $processOutput : null,\n        );\n    }\n\n    \/**\n     * @return array<string, string>\n     *\/\n    private function statusSections(): array\n    {\n        return [\n            'escaped' => 'escaped',\n            'timeouted' => 'timed out',\n            'killed' => 'killed',\n            'killedByStaticAnalysis' => 'killed by SA',\n            'errored' => 'error',\n            'syntaxErrors' => 'syntax error',\n            'uncovered' => 'not covered',\n            'ignored' => 'ignored',\n        ];\n    }\n\n    private function displayPath(string $path): string\n    {\n        $normalizedPath = str_replace('\\\\', '\/', $path);\n        $realProjectRoot = realpath($this->projectRoot);\n        $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n        if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n            \n        }\n\n        return ltrim($normalizedPath, '.\/');\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionReportParser.php","originalStartLine":157},"diff":"@@ @@\n         $normalizedRoot = rtrim(str_replace('\\\\', '\/', is_string($realProjectRoot) ? $realProjectRoot : $this->projectRoot), '\/');\n\n         if (str_starts_with($normalizedPath, $normalizedRoot . '\/')) {\n-            return substr($normalizedPath, strlen($normalizedRoot) + 1);\n+\n         }\n\n         return ltrim($normalizedPath, '.\/');","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.11df5d21c4b4de67db2b303dd7c0ab87.infection.xml\n\nF\n\nTime: 00:00.004, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testParsesFullInfectionJsonReport\nFailed asserting that two strings are identical.\n--- Expected\n+++ Actual\n@@ @@\n-'tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n+'home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Fixtures\/M14\/Infection\/infection-valid.json'\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:25\n\nFAILURES!\nTests: 1, Assertions: 1, Failures: 1."},{"mutator":{"mutatorName":"PublicVisibility","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    protected function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":13},"diff":"@@ @@\n\n final readonly class InfectionRunner\n {\n-    public function run(\n+    protected function run(\n         string $projectRoot,\n         string $binary,\n         ?string $configPath,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.606db0931ed6de5966ec7e33724f9cd0.infection.xml\n\n..E\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nError: Call to protected method GruffPhp\\Mutation\\InfectionRunner::run() from scope GruffPhp\\Tests\\Mutation\\InfectionReportParserTest\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary !== null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":22},"diff":"@@ @@\n     {\n         $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n-        if ($resolvedBinary === null) {\n+        if ($resolvedBinary !== null) {\n             return new InfectionRunResult(\n                 exitCode: 2,\n                 output: '',","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.fa4523ebfae9e8cce8dab3fe91ce0352.infection.xml\n\n..F\n\nTime: 00:00.008, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"ArrayItemRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = ['run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":36},"diff":"@@ @@\n         }\n\n         $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n-        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n+        $command = ['run', '--no-progress', '--log-verbosity=none'];\n\n         if ($effectiveConfigPath !== null) {\n             $command[] = '--configuration';","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.29c21b5e7abc83d69cd27f2c070ced18.infection.xml\n\n..F\n\nTime: 00:00.008, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 127 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath === null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":38},"diff":"@@ @@\n         $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n         $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n-        if ($effectiveConfigPath !== null) {\n+        if ($effectiveConfigPath === null) {\n             $command[] = '--configuration';\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.23b7fe74333784066d653d8445d9ecd4.infection.xml\n\n..E\n\nTime: 00:00.007, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nTypeError: GruffPhp\\Mutation\\InfectionRunner::absolutePath(): Argument #2 ($path) must be of type string, null given, called in \/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php on line 40\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:86\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:40\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions === null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if ($testFrameworkOptions === null && trim($testFrameworkOptions) !== '') {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f98a409669a9eb11710876e465890060.infection.xml\n\n..E\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nTypeError: trim(): Argument #1 ($string) must be of type string, null given\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:43\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"NotIdentical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) === '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) === '') {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a7039387ce7f865c212d7ac88842e820.infection.xml\n\n....F                                                               5 \/ 5 (100%)\n\nTime: 00:00.012, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPassesTestFrameworkOptions\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --test-framework-options=--testsuite=unit --filter=ExampleTest\" [ASCII](length: 117).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:111\n\nFAILURES!\nTests: 5, Assertions: 31, Failures: 1."},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null || trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if ($testFrameworkOptions !== null || trim($testFrameworkOptions) !== '') {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a5084adefd6abba6d229e50653148fd4.infection.xml\n\n..E\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nTypeError: trim(): Argument #1 ($string) must be of type string, null given\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:43\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"LogicalAndAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if (!($testFrameworkOptions !== null) && !(trim($testFrameworkOptions) !== '')) {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if (!($testFrameworkOptions !== null) && !(trim($testFrameworkOptions) !== '')) {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.7382d32971bc45d0954d312112610127.infection.xml\n\n..E\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nTypeError: trim(): Argument #1 ($string) must be of type string, null given\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:43\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if (!($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '')) {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":43},"diff":"@@ @@\n             $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n         }\n\n-        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n+        if (!($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '')) {\n             $command[] = '--test-framework-options=' . $testFrameworkOptions;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.95e0c20e4eb022f1c1cce0888260c2e9.infection.xml\n\n....F                                                               5 \/ 5 (100%)\n\nTime: 00:00.013, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPassesTestFrameworkOptions\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --test-framework-options=--testsuite=unit --filter=ExampleTest\" [ASCII](length: 117).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:111\n\nFAILURES!\nTests: 5, Assertions: 31, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = $testFrameworkOptions . '--test-framework-options=';\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":44},"diff":"@@ @@\n         }\n\n         if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n-            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n+            $command[] = $testFrameworkOptions . '--test-framework-options=';\n         }\n\n         $process = new Process($command, $projectRoot);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.e45d5487d71bd0e5f3a4d9e10b427a98.infection.xml\n\n....F                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPassesTestFrameworkOptions\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --testsuite=unit --filter=ExampleTest--test-framework-options=' [ASCII](length: 117) contains \"local-infection run --no-progress --log-verbosity=none --test-framework-options=--testsuite=unit --filter=ExampleTest\" [ASCII](length: 117).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:111\n\nFAILURES!\nTests: 5, Assertions: 31, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":44},"diff":"@@ @@\n         }\n\n         if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n-            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n+            $command[] = $testFrameworkOptions;\n         }\n\n         $process = new Process($command, $projectRoot);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dbc489c6d4e5894fde2e80bb0ded5b20.infection.xml\n\n....F                                                               5 \/ 5 (100%)\n\nTime: 00:00.011, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPassesTestFrameworkOptions\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --testsuite=unit --filter=ExampleTest' [ASCII](length: 92) contains \"local-infection run --no-progress --log-verbosity=none --test-framework-options=--testsuite=unit --filter=ExampleTest\" [ASCII](length: 117).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:111\n\nFAILURES!\nTests: 5, Assertions: 31, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=';\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":44},"diff":"@@ @@\n         }\n\n         if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n-            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n+            $command[] = '--test-framework-options=';\n         }\n\n         $process = new Process($command, $projectRoot);","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.14b28ba1769395c9d0614f541f31914c.infection.xml\n\n....F                                                               5 \/ 5 (100%)\n\nTime: 00:00.012, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPassesTestFrameworkOptions\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --test-framework-options=' [ASCII](length: 80) contains \"local-infection run --no-progress --log-verbosity=none --test-framework-options=--testsuite=unit --filter=ExampleTest\" [ASCII](length: 117).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:111\n\nFAILURES!\nTests: 5, Assertions: 31, Failures: 1."},{"mutator":{"mutatorName":"MethodCallRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        \n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":49},"diff":"@@ @@\n\n         $process = new Process($command, $projectRoot);\n         $process->setTimeout(null);\n-        $process->run();\n+\n\n         return new InfectionRunResult(\n             exitCode: $process->getExitCode() ?? 2,","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.64df5dabaefb936dce0ad71af583949c.infection.xml\n\n..E\n\nTime: 00:00.007, Memory: 10.00 MB\n\nThere was 1 error:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nSymfony\\Component\\Process\\Exception\\LogicException: Process must be started before calling \"getOutput()\".\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/vendor\/symfony\/process\/Process.php:1635\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/vendor\/symfony\/process\/Process.php:1404\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/vendor\/symfony\/process\/Process.php:627\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php:53\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:59\n\nERRORS!\nTests: 3, Assertions: 16, Errors: 1."},{"mutator":{"mutatorName":"Coalesce","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: 2 ?? $process->getExitCode(),\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":52},"diff":"@@ @@\n         $process->run();\n\n         return new InfectionRunResult(\n-            exitCode: $process->getExitCode() ?? 2,\n+            exitCode: 2 ?? $process->getExitCode(),\n             output: $process->getOutput(),\n             errorOutput: $process->getErrorOutput(),\n         );","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.581858bef9f84c1bbd990cd38e090da4.infection.xml\n\n..F\n\nTime: 00:00.009, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary !== '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":60},"diff":"@@ @@\n\n     private function resolveBinary(string $projectRoot, string $binary): ?string\n     {\n-        if ($binary === '') {\n+        if ($binary !== '') {\n             return null;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3d0be2deff5a24c2658b4ea6c1805bd9.infection.xml\n\n..F\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"LogicalOrAllSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (!str_contains($binary, '\/') || !str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":64},"diff":"@@ @@\n             return null;\n         }\n\n-        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n+        if (!str_contains($binary, '\/') || !str_contains($binary, '\\\\')) {\n             $candidate = $this->absolutePath($projectRoot, $binary);\n\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.dd676a55cd6d5f888509a9849bc6a3ec.infection.xml\n\n..F\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"LogicalOrNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (!(str_contains($binary, '\/') || str_contains($binary, '\\\\'))) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":64},"diff":"@@ @@\n             return null;\n         }\n\n-        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n+        if (!(str_contains($binary, '\/') || str_contains($binary, '\\\\'))) {\n             $candidate = $this->absolutePath($projectRoot, $binary);\n\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.5b8582bafeaf7bfdfcd40141e18d4578.infection.xml\n\n..F\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (!str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":64},"diff":"@@ @@\n             return null;\n         }\n\n-        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n+        if (!str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n             $candidate = $this->absolutePath($projectRoot, $binary);\n\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6578b582ddf0fb549f0a85a336826149.infection.xml\n\n..F\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"LogicalOrSingleSubExprNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || !str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":64},"diff":"@@ @@\n             return null;\n         }\n\n-        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n+        if (str_contains($binary, '\/') || !str_contains($binary, '\\\\')) {\n             $candidate = $this->absolutePath($projectRoot, $binary);\n\n             return is_file($candidate) && is_executable($candidate) ? $candidate : null;","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.ab7eb8ef2f0cde542bbffd5430fb6f5a.infection.xml\n\n..F\n\nTime: 00:00.006, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerPrefersProjectVendorBinary\nFailed asserting that 2 is identical to 0.\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:61\n\nFAILURES!\nTests: 3, Assertions: 17, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file('\/infection.json5' . rtrim($projectRoot, '\/')) ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":83},"diff":"@@ @@\n\n     private function defaultConfigPath(string $projectRoot): ?string\n     {\n-        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n+        return is_file('\/infection.json5' . rtrim($projectRoot, '\/')) ? 'infection.json5' : null;\n     }\n\n     private function absolutePath(string $projectRoot, string $path): string","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.a28cd710541c830cd2c6c4d42e13fe61.infection.xml\n\n...F\n\nTime: 00:00.010, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-0741044d93d5\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file('\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":83},"diff":"@@ @@\n\n     private function defaultConfigPath(string $projectRoot): ?string\n     {\n-        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n+        return is_file('\/infection.json5') ? 'infection.json5' : null;\n     }\n\n     private function absolutePath(string $projectRoot, string $path): string","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.6b53bc9f620df5cda577a96b8e766e89.infection.xml\n\n...F\n\nTime: 00:00.013, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-f8f6ca3e0cc4\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/')) ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":83},"diff":"@@ @@\n\n     private function defaultConfigPath(string $projectRoot): ?string\n     {\n-        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n+        return is_file(rtrim($projectRoot, '\/')) ? 'infection.json5' : null;\n     }\n\n     private function absolutePath(string $projectRoot, string $path): string","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.4fe0ca018d5a4b203a720983ca5e3283.infection.xml\n\n...F\n\nTime: 00:00.010, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-57d17695cf19\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"Ternary","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? null : 'infection.json5';\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":83},"diff":"@@ @@\n\n     private function defaultConfigPath(string $projectRoot): ?string\n     {\n-        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n+        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? null : 'infection.json5';\n     }\n\n     private function absolutePath(string $projectRoot, string $path): string","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.8a4a51725df2c55aef1c7bae8746e1a1.infection.xml\n\n...F\n\nTime: 00:00.009, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none' [ASCII](length: 54) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-3d2f10b15dd3\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"Identical","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] !== '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if ($path !== '' && $path[0] !== '\/') {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.3999387e9424c5c613ff13a8e3592264.infection.xml\n\n...F\n\nTime: 00:00.012, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration infection.json5' [ASCII](length: 86) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-9fc8867cb8a0\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"LogicalAnd","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' || $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if ($path !== '' || $path[0] === '\/') {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.061c87793a22dcbee1ecf6999f99809e.infection.xml\n\n...F\n\nTime: 00:00.012, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration infection.json5' [ASCII](length: 86) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-755d820bc91e\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"LogicalAndNegation","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if (!($path !== '' && $path[0] === '\/')) {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":88},"diff":"@@ @@\n\n     private function absolutePath(string $projectRoot, string $path): string\n     {\n-        if ($path !== '' && $path[0] === '\/') {\n+        if (!($path !== '' && $path[0] === '\/')) {\n             return $path;\n         }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f065adf14f189d8a5ea96f98c7ba2fac.infection.xml\n\n...F\n\nTime: 00:00.013, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration infection.json5' [ASCII](length: 86) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-f69cdc4929c5\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return '\/' . rtrim($projectRoot, '\/') . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return '\/' . rtrim($projectRoot, '\/') . $path;\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.4cf59aff91d8e7b5a361f31ad932570f.infection.xml\n\n...F\n\nTime: 00:00.012, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration \/\/tmp\/gruff-infection-runner-9a2df58308feinfection.json5' [ASCII](length: 127) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-9a2df58308fe\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return '\/' . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return '\/' . $path;\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.2e066eed3184472f0f9710a7842bcb62.infection.xml\n\n...F\n\nTime: 00:00.011, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration \/infection.json5' [ASCII](length: 87) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-79ed65e13c19\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . $path;\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return rtrim($projectRoot, '\/') . $path;\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.1382ad46fb4159b8fb5ed87bd9cbcef6.infection.xml\n\n...F\n\nTime: 00:00.010, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-e58308269846infection.json5' [ASCII](length: 126) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-e58308269846\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"Concat","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . $path . '\/';\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return rtrim($projectRoot, '\/') . $path . '\/';\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.f394341e14a8056068485f8bc5c879f6.infection.xml\n\n...F\n\nTime: 00:00.014, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-3b6428373e31infection.json5\/' [ASCII](length: 127) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-3b6428373e31\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."},{"mutator":{"mutatorName":"ConcatOperandRemoval","originalSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/' . $path;\n    }\n}\n","mutatedSourceCode":"<?php\n\ndeclare(strict_types=1);\n\nnamespace GruffPhp\\Mutation;\n\nuse GruffPhp\\Analysis\\RunDiagnostic;\nuse Symfony\\Component\\Process\\ExecutableFinder;\nuse Symfony\\Component\\Process\\Process;\n\nfinal readonly class InfectionRunner\n{\n    public function run(\n        string $projectRoot,\n        string $binary,\n        ?string $configPath,\n        ?string $testFrameworkOptions = null,\n    ): InfectionRunResult\n    {\n        $resolvedBinary = $this->resolveBinary($projectRoot, $binary);\n\n        if ($resolvedBinary === null) {\n            return new InfectionRunResult(\n                exitCode: 2,\n                output: '',\n                errorOutput: '',\n                diagnostic: new RunDiagnostic(\n                    type: 'mutation-tool-error',\n                    message: sprintf('Infection executable not found: %s', $binary),\n                    path: $binary,\n                ),\n            );\n        }\n\n        $effectiveConfigPath = $configPath ?? $this->defaultConfigPath($projectRoot);\n        $command = [$resolvedBinary, 'run', '--no-progress', '--log-verbosity=none'];\n\n        if ($effectiveConfigPath !== null) {\n            $command[] = '--configuration';\n            $command[] = $this->absolutePath($projectRoot, $effectiveConfigPath);\n        }\n\n        if ($testFrameworkOptions !== null && trim($testFrameworkOptions) !== '') {\n            $command[] = '--test-framework-options=' . $testFrameworkOptions;\n        }\n\n        $process = new Process($command, $projectRoot);\n        $process->setTimeout(null);\n        $process->run();\n\n        return new InfectionRunResult(\n            exitCode: $process->getExitCode() ?? 2,\n            output: $process->getOutput(),\n            errorOutput: $process->getErrorOutput(),\n        );\n    }\n\n    private function resolveBinary(string $projectRoot, string $binary): ?string\n    {\n        if ($binary === '') {\n            return null;\n        }\n\n        if (str_contains($binary, '\/') || str_contains($binary, '\\\\')) {\n            $candidate = $this->absolutePath($projectRoot, $binary);\n\n            return is_file($candidate) && is_executable($candidate) ? $candidate : null;\n        }\n\n        $localBinary = rtrim($projectRoot, '\/') . '\/vendor\/bin\/' . $binary;\n\n        if (is_file($localBinary) && is_executable($localBinary)) {\n            return $localBinary;\n        }\n\n        $resolved = (new ExecutableFinder())->find($binary);\n\n        return is_string($resolved) ? $resolved : null;\n    }\n\n    private function defaultConfigPath(string $projectRoot): ?string\n    {\n        return is_file(rtrim($projectRoot, '\/') . '\/infection.json5') ? 'infection.json5' : null;\n    }\n\n    private function absolutePath(string $projectRoot, string $path): string\n    {\n        if ($path !== '' && $path[0] === '\/') {\n            return $path;\n        }\n\n        return rtrim($projectRoot, '\/') . '\/';\n    }\n}\n","originalFilePath":"\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/src\/Mutation\/InfectionRunner.php","originalStartLine":92},"diff":"@@ @@\n             return $path;\n         }\n\n-        return rtrim($projectRoot, '\/') . '\/' . $path;\n+        return rtrim($projectRoot, '\/') . '\/';\n     }\n }","processOutput":"PHPUnit 11.5.55 by Sebastian Bergmann and contributors.\n\nRuntime:       PHP 8.3.30\nConfiguration: \/tmp\/infection\/phpunitConfiguration.fe9dbb53ee1a13257aeade9cb6b5cf37.infection.xml\n\n...F\n\nTime: 00:00.010, Memory: 10.00 MB\n\nThere was 1 failure:\n\n1) GruffPhp\\Tests\\Mutation\\InfectionReportParserTest::testInfectionRunnerUsesProjectInfectionConfigByDefault\nFailed asserting that 'local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-3f8788ce8887\/' [ASCII](length: 112) contains \"local-infection run --no-progress --log-verbosity=none --configuration \/tmp\/gruff-infection-runner-3f8788ce8887\/infection.json5\" [ASCII](length: 127).\n\n\/home\/devgoat\/projects\/gruff-workspace\/gruff-php\/tests\/Mutation\/InfectionReportParserTest.php:83\n\nFAILURES!\nTests: 4, Assertions: 25, Failures: 1."}],"killedByStaticAnalysis":[],"errored":[],"syntaxErrors":[],"uncovered":[],"ignored":[]}